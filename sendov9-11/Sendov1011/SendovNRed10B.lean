import Mathlib
import SendovNEmajEq
import SendovNRows10
import SendovNCoverGrid
import Sendov911Box10_50
import Sendov911Box10_51
import Sendov911Box10_52
import Sendov911Box10_53
import Sendov911Box10_54
import Sendov911Box10_55
import Sendov911Box10_56
import Sendov911Box10_57
import Sendov911Box10_58
import Sendov911Box10_59

set_option maxHeartbeats 4000000000
set_option maxRecDepth 100000

/-!
# D10-6/7 assembly chunk B (rows 50–59; generated by
emit_assembly1011.py, do not hand-edit)

Per row: the `Box` value, the hEq identity against the shipped monomial table
(regenerated from the verifier arithmetic and matched integer-for-integer
before emission), and the row reduction `red_i` (`row_nonpos` → pullback).
-/

namespace SendovN.Red10B

open Finset Sendov911Capstone

/-! ### Row 50: `a ∈ [1/2, 51/100]`, S = 3761965961094050726256930261574837525347587761/544265570250000000000000000000000000000000000, λ = 42329/50000, Y = 53227/100000 -/

noncomputable def box_50 : Box where
  nx := 10
  ny := 10
  A := Sendov911Box10_50.tab Sendov911Box10_50.Adata
  D := (Sendov911Box10_50.Dscale : ℝ)
  hD := by norm_num [Sendov911Box10_50.Dscale]

theorem eq_50 (U V : ℝ) :
    (1 - ((1/2 : ℝ) + (1/100) * U)) / (((9:ℕ):ℝ) + 1) + ((53227/100000 : ℝ) * V) ^ 2 / (2 * (((9:ℕ):ℝ) + 1))
      - MidChainN.hh 9 (3761965961094050726256930261574837525347587761/544265570250000000000000000000000000000000000 : ℝ) ((1/2 : ℝ) + (1/100) * U) ((53227/100000 : ℝ) * V) ^ 5 / ((((9:ℕ):ℝ) + 1) * (42329/50000 : ℝ))
      - MidChainN.Emaj 9 CTab10.c10 (3761965961094050726256930261574837525347587761/544265570250000000000000000000000000000000000 : ℝ) ((1/2 : ℝ) + (1/100) * U) ((53227/100000 : ℝ) * V)
    = ∑ k ∈ Finset.range 11, ∑ l ∈ Finset.range 11,
        ((Sendov911Box10_50.tab Sendov911Box10_50.Adata k l : ℝ)
          / (Sendov911Box10_50.Dscale : ℝ)) * U ^ k * V ^ l := by
  rw [EmajEq.Emaj10_eq]
  unfold MidChainN.hh
  push_cast
  simp only [Finset.sum_range_succ, Finset.sum_range_zero,
    Sendov911Box10_50.tab, Sendov911Box10_50.Adata, Sendov911Box10_50.Dscale,
    List.getD_cons_zero, List.getD_cons_succ, zero_add]
  norm_num
  ring

theorem red_50 (mu : ℂ) (Df : Fin 9 → ℂ) (rr : Fin 9 → ℝ) (a eta sig : ℝ)
    (h0 : (1/2 : ℝ) ≤ a) (h1 : a ≤ (51/100 : ℝ))
    (heta0 : 0 ≤ eta) (hnormSq : Complex.normSq mu = 1 - eta ^ 2)
    (hloc : (9:ℝ) ≤ 9 * a * mu.re + (1 - a ^ 2) * sig)
    (hsigeq : sig = ∑ k, 1 / rr k ^ 2)
    (hrlo : ∀ k, (309/500 : ℝ) ≤ rr k) (hrhi : ∀ k, rr k ≤ a + 1)
    (hprod : (10:ℝ) ≤ ∏ k, rr k)
    (hz : ∑ j, Df j = 0)
    (hD2 : ∑ j, ‖Df j‖ ^ 2 ≤ 9 * eta ^ 2)
    (hI : ‖∫ t in (0:ℝ)..a, ∏ j, ((1:ℂ) - (t:ℂ) * (mu + Df j))‖ < a / 10) :
    ∃ U V : ℝ, 0 ≤ U ∧ U ≤ 1 ∧ 0 ≤ V ∧ V ≤ 1 ∧
      Sendov911Capstone.Box.poly box_50 U V ≤ 0 := by
  have ha0 : (0:ℝ) < a := by linarith
  have ha1 : a ≤ 1 := by linarith
  have hsig : sig ≤ (3761965961094050726256930261574837525347587761/544265570250000000000000000000000000000000000 : ℝ) := by
    rw [hsigeq]
    exact Rows10.sigma_row_50 rr hrlo (fun k => le_trans (hrhi k) (by linarith)) hprod
  have hL := Rows10.L_row_50 h0 h1
  have hloc' : ((9:ℕ):ℝ) ≤ ((9:ℕ):ℝ) * a * mu.re + (1 - a ^ 2) * sig := by
    push_cast
    exact hloc
  have hkey := MidChainN.key_of_loc ha1 ha0.le hloc' hsig
  have hL' : 0 ≤ (3761965961094050726256930261574837525347587761/544265570250000000000000000000000000000000000 : ℝ) * a ^ 2 - ((9:ℕ):ℝ) * (42329/50000 : ℝ) * a + ((9:ℕ):ℝ) - (3761965961094050726256930261574837525347587761/544265570250000000000000000000000000000000000 : ℝ) := by
    push_cast
    exact hL
  have hlamu := MidChainN.lam_le_u (by norm_num) ha0 hkey hL'
  have hmunorm := MidChainN.lam_le_norm mu hlamu
  have hmu0 : mu ≠ 0 := by
    intro hcon
    rw [hcon] at hmunorm
    simp only [norm_zero] at hmunorm
    norm_num at hmunorm
  have hetale := MidChainN.eta_sq_le mu (by norm_num) hnormSq hmunorm
  have hetaY : eta ≤ (53227/100000 : ℝ) :=
    MidChainN.eta_le_Y (by norm_num) heta0 hetale Rows10.Y_row_50
  have hP := MidChainN.row_nonpos (N := 9) (alpha := (1/2 : ℝ)) (beta := (51/100 : ℝ))
    (S := (3761965961094050726256930261574837525347587761/544265570250000000000000000000000000000000000 : ℝ)) (lam := (42329/50000 : ℝ)) (sigma := sig) (by norm_num)
    CTab10.c10 (fun m _ => CTab10.c10_nonneg m) mu Df a eta
    (by norm_num) (by norm_num) (by norm_num) Rows10.lam_row_50 h0 h1
    heta0 hnormSq hloc' hsig hL' hz
    (CTab10.cTable10 Df eta heta0 hz hD2)
    (by push_cast; norm_num; exact JBoundN.J_closed_form_deg10 hmu0 a)
    (by push_cast; norm_num; exact hI)
  refine ⟨100 * (a - (1/2 : ℝ)), eta / (53227/100000 : ℝ), by linarith, by linarith,
    div_nonneg heta0 (by norm_num), (div_le_one (by norm_num)).mpr hetaY, ?_⟩
  have heq := eq_50 (100 * (a - (1/2 : ℝ))) (eta / (53227/100000 : ℝ))
  rw [show (1/2 : ℝ) + (1/100) * (100 * (a - (1/2 : ℝ))) = a by ring] at heq
  rw [show (53227/100000 : ℝ) * (eta / (53227/100000 : ℝ)) = eta by field_simp <;> ring] at heq
  exact le_of_eq_of_le heq.symm hP

/-! ### Row 51: `a ∈ [51/100, 13/25]`, S = 14024725670822573781864915008112559/2006338967476040124893188476562500, λ = 20833/25000, Y = 691/1250 -/

noncomputable def box_51 : Box where
  nx := 10
  ny := 10
  A := Sendov911Box10_51.tab Sendov911Box10_51.Adata
  D := (Sendov911Box10_51.Dscale : ℝ)
  hD := by norm_num [Sendov911Box10_51.Dscale]

theorem eq_51 (U V : ℝ) :
    (1 - ((51/100 : ℝ) + (1/100) * U)) / (((9:ℕ):ℝ) + 1) + ((691/1250 : ℝ) * V) ^ 2 / (2 * (((9:ℕ):ℝ) + 1))
      - MidChainN.hh 9 (14024725670822573781864915008112559/2006338967476040124893188476562500 : ℝ) ((51/100 : ℝ) + (1/100) * U) ((691/1250 : ℝ) * V) ^ 5 / ((((9:ℕ):ℝ) + 1) * (20833/25000 : ℝ))
      - MidChainN.Emaj 9 CTab10.c10 (14024725670822573781864915008112559/2006338967476040124893188476562500 : ℝ) ((51/100 : ℝ) + (1/100) * U) ((691/1250 : ℝ) * V)
    = ∑ k ∈ Finset.range 11, ∑ l ∈ Finset.range 11,
        ((Sendov911Box10_51.tab Sendov911Box10_51.Adata k l : ℝ)
          / (Sendov911Box10_51.Dscale : ℝ)) * U ^ k * V ^ l := by
  rw [EmajEq.Emaj10_eq]
  unfold MidChainN.hh
  push_cast
  simp only [Finset.sum_range_succ, Finset.sum_range_zero,
    Sendov911Box10_51.tab, Sendov911Box10_51.Adata, Sendov911Box10_51.Dscale,
    List.getD_cons_zero, List.getD_cons_succ, zero_add]
  norm_num
  ring

theorem red_51 (mu : ℂ) (Df : Fin 9 → ℂ) (rr : Fin 9 → ℝ) (a eta sig : ℝ)
    (h0 : (51/100 : ℝ) ≤ a) (h1 : a ≤ (13/25 : ℝ))
    (heta0 : 0 ≤ eta) (hnormSq : Complex.normSq mu = 1 - eta ^ 2)
    (hloc : (9:ℝ) ≤ 9 * a * mu.re + (1 - a ^ 2) * sig)
    (hsigeq : sig = ∑ k, 1 / rr k ^ 2)
    (hrlo : ∀ k, (309/500 : ℝ) ≤ rr k) (hrhi : ∀ k, rr k ≤ a + 1)
    (hprod : (10:ℝ) ≤ ∏ k, rr k)
    (hz : ∑ j, Df j = 0)
    (hD2 : ∑ j, ‖Df j‖ ^ 2 ≤ 9 * eta ^ 2)
    (hI : ‖∫ t in (0:ℝ)..a, ∏ j, ((1:ℂ) - (t:ℂ) * (mu + Df j))‖ < a / 10) :
    ∃ U V : ℝ, 0 ≤ U ∧ U ≤ 1 ∧ 0 ≤ V ∧ V ≤ 1 ∧
      Sendov911Capstone.Box.poly box_51 U V ≤ 0 := by
  have ha0 : (0:ℝ) < a := by linarith
  have ha1 : a ≤ 1 := by linarith
  have hsig : sig ≤ (14024725670822573781864915008112559/2006338967476040124893188476562500 : ℝ) := by
    rw [hsigeq]
    exact Rows10.sigma_row_51 rr hrlo (fun k => le_trans (hrhi k) (by linarith)) hprod
  have hL := Rows10.L_row_51 h0 h1
  have hloc' : ((9:ℕ):ℝ) ≤ ((9:ℕ):ℝ) * a * mu.re + (1 - a ^ 2) * sig := by
    push_cast
    exact hloc
  have hkey := MidChainN.key_of_loc ha1 ha0.le hloc' hsig
  have hL' : 0 ≤ (14024725670822573781864915008112559/2006338967476040124893188476562500 : ℝ) * a ^ 2 - ((9:ℕ):ℝ) * (20833/25000 : ℝ) * a + ((9:ℕ):ℝ) - (14024725670822573781864915008112559/2006338967476040124893188476562500 : ℝ) := by
    push_cast
    exact hL
  have hlamu := MidChainN.lam_le_u (by norm_num) ha0 hkey hL'
  have hmunorm := MidChainN.lam_le_norm mu hlamu
  have hmu0 : mu ≠ 0 := by
    intro hcon
    rw [hcon] at hmunorm
    simp only [norm_zero] at hmunorm
    norm_num at hmunorm
  have hetale := MidChainN.eta_sq_le mu (by norm_num) hnormSq hmunorm
  have hetaY : eta ≤ (691/1250 : ℝ) :=
    MidChainN.eta_le_Y (by norm_num) heta0 hetale Rows10.Y_row_51
  have hP := MidChainN.row_nonpos (N := 9) (alpha := (51/100 : ℝ)) (beta := (13/25 : ℝ))
    (S := (14024725670822573781864915008112559/2006338967476040124893188476562500 : ℝ)) (lam := (20833/25000 : ℝ)) (sigma := sig) (by norm_num)
    CTab10.c10 (fun m _ => CTab10.c10_nonneg m) mu Df a eta
    (by norm_num) (by norm_num) (by norm_num) Rows10.lam_row_51 h0 h1
    heta0 hnormSq hloc' hsig hL' hz
    (CTab10.cTable10 Df eta heta0 hz hD2)
    (by push_cast; norm_num; exact JBoundN.J_closed_form_deg10 hmu0 a)
    (by push_cast; norm_num; exact hI)
  refine ⟨100 * (a - (51/100 : ℝ)), eta / (691/1250 : ℝ), by linarith, by linarith,
    div_nonneg heta0 (by norm_num), (div_le_one (by norm_num)).mpr hetaY, ?_⟩
  have heq := eq_51 (100 * (a - (51/100 : ℝ))) (eta / (691/1250 : ℝ))
  rw [show (51/100 : ℝ) + (1/100) * (100 * (a - (51/100 : ℝ))) = a by ring] at heq
  rw [show (691/1250 : ℝ) * (eta / (691/1250 : ℝ)) = eta by field_simp <;> ring] at heq
  exact le_of_eq_of_le heq.symm hP

/-! ### Row 52: `a ∈ [13/25, 53/100]`, S = 439558589632411117530555774191919265122748009/62086520250000000000000000000000000000000000, λ = 40967/50000, Y = 57331/100000 -/

noncomputable def box_52 : Box where
  nx := 10
  ny := 10
  A := Sendov911Box10_52.tab Sendov911Box10_52.Adata
  D := (Sendov911Box10_52.Dscale : ℝ)
  hD := by norm_num [Sendov911Box10_52.Dscale]

theorem eq_52 (U V : ℝ) :
    (1 - ((13/25 : ℝ) + (1/100) * U)) / (((9:ℕ):ℝ) + 1) + ((57331/100000 : ℝ) * V) ^ 2 / (2 * (((9:ℕ):ℝ) + 1))
      - MidChainN.hh 9 (439558589632411117530555774191919265122748009/62086520250000000000000000000000000000000000 : ℝ) ((13/25 : ℝ) + (1/100) * U) ((57331/100000 : ℝ) * V) ^ 5 / ((((9:ℕ):ℝ) + 1) * (40967/50000 : ℝ))
      - MidChainN.Emaj 9 CTab10.c10 (439558589632411117530555774191919265122748009/62086520250000000000000000000000000000000000 : ℝ) ((13/25 : ℝ) + (1/100) * U) ((57331/100000 : ℝ) * V)
    = ∑ k ∈ Finset.range 11, ∑ l ∈ Finset.range 11,
        ((Sendov911Box10_52.tab Sendov911Box10_52.Adata k l : ℝ)
          / (Sendov911Box10_52.Dscale : ℝ)) * U ^ k * V ^ l := by
  rw [EmajEq.Emaj10_eq]
  unfold MidChainN.hh
  push_cast
  simp only [Finset.sum_range_succ, Finset.sum_range_zero,
    Sendov911Box10_52.tab, Sendov911Box10_52.Adata, Sendov911Box10_52.Dscale,
    List.getD_cons_zero, List.getD_cons_succ, zero_add]
  norm_num
  ring

theorem red_52 (mu : ℂ) (Df : Fin 9 → ℂ) (rr : Fin 9 → ℝ) (a eta sig : ℝ)
    (h0 : (13/25 : ℝ) ≤ a) (h1 : a ≤ (53/100 : ℝ))
    (heta0 : 0 ≤ eta) (hnormSq : Complex.normSq mu = 1 - eta ^ 2)
    (hloc : (9:ℝ) ≤ 9 * a * mu.re + (1 - a ^ 2) * sig)
    (hsigeq : sig = ∑ k, 1 / rr k ^ 2)
    (hrlo : ∀ k, (309/500 : ℝ) ≤ rr k) (hrhi : ∀ k, rr k ≤ a + 1)
    (hprod : (10:ℝ) ≤ ∏ k, rr k)
    (hz : ∑ j, Df j = 0)
    (hD2 : ∑ j, ‖Df j‖ ^ 2 ≤ 9 * eta ^ 2)
    (hI : ‖∫ t in (0:ℝ)..a, ∏ j, ((1:ℂ) - (t:ℂ) * (mu + Df j))‖ < a / 10) :
    ∃ U V : ℝ, 0 ≤ U ∧ U ≤ 1 ∧ 0 ≤ V ∧ V ≤ 1 ∧
      Sendov911Capstone.Box.poly box_52 U V ≤ 0 := by
  have ha0 : (0:ℝ) < a := by linarith
  have ha1 : a ≤ 1 := by linarith
  have hsig : sig ≤ (439558589632411117530555774191919265122748009/62086520250000000000000000000000000000000000 : ℝ) := by
    rw [hsigeq]
    exact Rows10.sigma_row_52 rr hrlo (fun k => le_trans (hrhi k) (by linarith)) hprod
  have hL := Rows10.L_row_52 h0 h1
  have hloc' : ((9:ℕ):ℝ) ≤ ((9:ℕ):ℝ) * a * mu.re + (1 - a ^ 2) * sig := by
    push_cast
    exact hloc
  have hkey := MidChainN.key_of_loc ha1 ha0.le hloc' hsig
  have hL' : 0 ≤ (439558589632411117530555774191919265122748009/62086520250000000000000000000000000000000000 : ℝ) * a ^ 2 - ((9:ℕ):ℝ) * (40967/50000 : ℝ) * a + ((9:ℕ):ℝ) - (439558589632411117530555774191919265122748009/62086520250000000000000000000000000000000000 : ℝ) := by
    push_cast
    exact hL
  have hlamu := MidChainN.lam_le_u (by norm_num) ha0 hkey hL'
  have hmunorm := MidChainN.lam_le_norm mu hlamu
  have hmu0 : mu ≠ 0 := by
    intro hcon
    rw [hcon] at hmunorm
    simp only [norm_zero] at hmunorm
    norm_num at hmunorm
  have hetale := MidChainN.eta_sq_le mu (by norm_num) hnormSq hmunorm
  have hetaY : eta ≤ (57331/100000 : ℝ) :=
    MidChainN.eta_le_Y (by norm_num) heta0 hetale Rows10.Y_row_52
  have hP := MidChainN.row_nonpos (N := 9) (alpha := (13/25 : ℝ)) (beta := (53/100 : ℝ))
    (S := (439558589632411117530555774191919265122748009/62086520250000000000000000000000000000000000 : ℝ)) (lam := (40967/50000 : ℝ)) (sigma := sig) (by norm_num)
    CTab10.c10 (fun m _ => CTab10.c10_nonneg m) mu Df a eta
    (by norm_num) (by norm_num) (by norm_num) Rows10.lam_row_52 h0 h1
    heta0 hnormSq hloc' hsig hL' hz
    (CTab10.cTable10 Df eta heta0 hz hD2)
    (by push_cast; norm_num; exact JBoundN.J_closed_form_deg10 hmu0 a)
    (by push_cast; norm_num; exact hI)
  refine ⟨100 * (a - (13/25 : ℝ)), eta / (57331/100000 : ℝ), by linarith, by linarith,
    div_nonneg heta0 (by norm_num), (div_le_one (by norm_num)).mpr hetaY, ?_⟩
  have heq := eq_52 (100 * (a - (13/25 : ℝ))) (eta / (57331/100000 : ℝ))
  rw [show (13/25 : ℝ) + (1/100) * (100 * (a - (13/25 : ℝ))) = a by ring] at heq
  rw [show (57331/100000 : ℝ) * (eta / (57331/100000 : ℝ)) = eta by field_simp <;> ring] at heq
  exact le_of_eq_of_le heq.symm hP

/-! ### Row 53: `a ∈ [53/100, 27/50]`, S = 8862143053186214519585771112498713436103/1234014999389648437500000000000000000000, λ = 80413/100000, Y = 29723/50000 -/

noncomputable def box_53 : Box where
  nx := 10
  ny := 10
  A := Sendov911Box10_53.tab Sendov911Box10_53.Adata
  D := (Sendov911Box10_53.Dscale : ℝ)
  hD := by norm_num [Sendov911Box10_53.Dscale]

theorem eq_53 (U V : ℝ) :
    (1 - ((53/100 : ℝ) + (1/100) * U)) / (((9:ℕ):ℝ) + 1) + ((29723/50000 : ℝ) * V) ^ 2 / (2 * (((9:ℕ):ℝ) + 1))
      - MidChainN.hh 9 (8862143053186214519585771112498713436103/1234014999389648437500000000000000000000 : ℝ) ((53/100 : ℝ) + (1/100) * U) ((29723/50000 : ℝ) * V) ^ 5 / ((((9:ℕ):ℝ) + 1) * (80413/100000 : ℝ))
      - MidChainN.Emaj 9 CTab10.c10 (8862143053186214519585771112498713436103/1234014999389648437500000000000000000000 : ℝ) ((53/100 : ℝ) + (1/100) * U) ((29723/50000 : ℝ) * V)
    = ∑ k ∈ Finset.range 11, ∑ l ∈ Finset.range 11,
        ((Sendov911Box10_53.tab Sendov911Box10_53.Adata k l : ℝ)
          / (Sendov911Box10_53.Dscale : ℝ)) * U ^ k * V ^ l := by
  rw [EmajEq.Emaj10_eq]
  unfold MidChainN.hh
  push_cast
  simp only [Finset.sum_range_succ, Finset.sum_range_zero,
    Sendov911Box10_53.tab, Sendov911Box10_53.Adata, Sendov911Box10_53.Dscale,
    List.getD_cons_zero, List.getD_cons_succ, zero_add]
  norm_num
  ring

theorem red_53 (mu : ℂ) (Df : Fin 9 → ℂ) (rr : Fin 9 → ℝ) (a eta sig : ℝ)
    (h0 : (53/100 : ℝ) ≤ a) (h1 : a ≤ (27/50 : ℝ))
    (heta0 : 0 ≤ eta) (hnormSq : Complex.normSq mu = 1 - eta ^ 2)
    (hloc : (9:ℝ) ≤ 9 * a * mu.re + (1 - a ^ 2) * sig)
    (hsigeq : sig = ∑ k, 1 / rr k ^ 2)
    (hrlo : ∀ k, (309/500 : ℝ) ≤ rr k) (hrhi : ∀ k, rr k ≤ a + 1)
    (hprod : (10:ℝ) ≤ ∏ k, rr k)
    (hz : ∑ j, Df j = 0)
    (hD2 : ∑ j, ‖Df j‖ ^ 2 ≤ 9 * eta ^ 2)
    (hI : ‖∫ t in (0:ℝ)..a, ∏ j, ((1:ℂ) - (t:ℂ) * (mu + Df j))‖ < a / 10) :
    ∃ U V : ℝ, 0 ≤ U ∧ U ≤ 1 ∧ 0 ≤ V ∧ V ≤ 1 ∧
      Sendov911Capstone.Box.poly box_53 U V ≤ 0 := by
  have ha0 : (0:ℝ) < a := by linarith
  have ha1 : a ≤ 1 := by linarith
  have hsig : sig ≤ (8862143053186214519585771112498713436103/1234014999389648437500000000000000000000 : ℝ) := by
    rw [hsigeq]
    exact Rows10.sigma_row_53 rr hrlo (fun k => le_trans (hrhi k) (by linarith)) hprod
  have hL := Rows10.L_row_53 h0 h1
  have hloc' : ((9:ℕ):ℝ) ≤ ((9:ℕ):ℝ) * a * mu.re + (1 - a ^ 2) * sig := by
    push_cast
    exact hloc
  have hkey := MidChainN.key_of_loc ha1 ha0.le hloc' hsig
  have hL' : 0 ≤ (8862143053186214519585771112498713436103/1234014999389648437500000000000000000000 : ℝ) * a ^ 2 - ((9:ℕ):ℝ) * (80413/100000 : ℝ) * a + ((9:ℕ):ℝ) - (8862143053186214519585771112498713436103/1234014999389648437500000000000000000000 : ℝ) := by
    push_cast
    exact hL
  have hlamu := MidChainN.lam_le_u (by norm_num) ha0 hkey hL'
  have hmunorm := MidChainN.lam_le_norm mu hlamu
  have hmu0 : mu ≠ 0 := by
    intro hcon
    rw [hcon] at hmunorm
    simp only [norm_zero] at hmunorm
    norm_num at hmunorm
  have hetale := MidChainN.eta_sq_le mu (by norm_num) hnormSq hmunorm
  have hetaY : eta ≤ (29723/50000 : ℝ) :=
    MidChainN.eta_le_Y (by norm_num) heta0 hetale Rows10.Y_row_53
  have hP := MidChainN.row_nonpos (N := 9) (alpha := (53/100 : ℝ)) (beta := (27/50 : ℝ))
    (S := (8862143053186214519585771112498713436103/1234014999389648437500000000000000000000 : ℝ)) (lam := (80413/100000 : ℝ)) (sigma := sig) (by norm_num)
    CTab10.c10 (fun m _ => CTab10.c10_nonneg m) mu Df a eta
    (by norm_num) (by norm_num) (by norm_num) Rows10.lam_row_53 h0 h1
    heta0 hnormSq hloc' hsig hL' hz
    (CTab10.cTable10 Df eta heta0 hz hD2)
    (by push_cast; norm_num; exact JBoundN.J_closed_form_deg10 hmu0 a)
    (by push_cast; norm_num; exact hI)
  refine ⟨100 * (a - (53/100 : ℝ)), eta / (29723/50000 : ℝ), by linarith, by linarith,
    div_nonneg heta0 (by norm_num), (div_le_one (by norm_num)).mpr hetaY, ?_⟩
  have heq := eq_53 (100 * (a - (53/100 : ℝ))) (eta / (29723/50000 : ℝ))
  rw [show (53/100 : ℝ) + (1/100) * (100 * (a - (53/100 : ℝ))) = a by ring] at heq
  rw [show (29723/50000 : ℝ) * (eta / (29723/50000 : ℝ)) = eta by field_simp <;> ring] at heq
  exact le_of_eq_of_le heq.symm hP

/-! ### Row 54: `a ∈ [27/50, 11/20]`, S = 27422806098205692817843150524782641/3758376591360000000000000000000000, λ = 78831/100000, Y = 7691/12500 -/

noncomputable def box_54 : Box where
  nx := 10
  ny := 10
  A := Sendov911Box10_54.tab Sendov911Box10_54.Adata
  D := (Sendov911Box10_54.Dscale : ℝ)
  hD := by norm_num [Sendov911Box10_54.Dscale]

theorem eq_54 (U V : ℝ) :
    (1 - ((27/50 : ℝ) + (1/100) * U)) / (((9:ℕ):ℝ) + 1) + ((7691/12500 : ℝ) * V) ^ 2 / (2 * (((9:ℕ):ℝ) + 1))
      - MidChainN.hh 9 (27422806098205692817843150524782641/3758376591360000000000000000000000 : ℝ) ((27/50 : ℝ) + (1/100) * U) ((7691/12500 : ℝ) * V) ^ 5 / ((((9:ℕ):ℝ) + 1) * (78831/100000 : ℝ))
      - MidChainN.Emaj 9 CTab10.c10 (27422806098205692817843150524782641/3758376591360000000000000000000000 : ℝ) ((27/50 : ℝ) + (1/100) * U) ((7691/12500 : ℝ) * V)
    = ∑ k ∈ Finset.range 11, ∑ l ∈ Finset.range 11,
        ((Sendov911Box10_54.tab Sendov911Box10_54.Adata k l : ℝ)
          / (Sendov911Box10_54.Dscale : ℝ)) * U ^ k * V ^ l := by
  rw [EmajEq.Emaj10_eq]
  unfold MidChainN.hh
  push_cast
  simp only [Finset.sum_range_succ, Finset.sum_range_zero,
    Sendov911Box10_54.tab, Sendov911Box10_54.Adata, Sendov911Box10_54.Dscale,
    List.getD_cons_zero, List.getD_cons_succ, zero_add]
  norm_num
  ring

theorem red_54 (mu : ℂ) (Df : Fin 9 → ℂ) (rr : Fin 9 → ℝ) (a eta sig : ℝ)
    (h0 : (27/50 : ℝ) ≤ a) (h1 : a ≤ (11/20 : ℝ))
    (heta0 : 0 ≤ eta) (hnormSq : Complex.normSq mu = 1 - eta ^ 2)
    (hloc : (9:ℝ) ≤ 9 * a * mu.re + (1 - a ^ 2) * sig)
    (hsigeq : sig = ∑ k, 1 / rr k ^ 2)
    (hrlo : ∀ k, (309/500 : ℝ) ≤ rr k) (hrhi : ∀ k, rr k ≤ a + 1)
    (hprod : (10:ℝ) ≤ ∏ k, rr k)
    (hz : ∑ j, Df j = 0)
    (hD2 : ∑ j, ‖Df j‖ ^ 2 ≤ 9 * eta ^ 2)
    (hI : ‖∫ t in (0:ℝ)..a, ∏ j, ((1:ℂ) - (t:ℂ) * (mu + Df j))‖ < a / 10) :
    ∃ U V : ℝ, 0 ≤ U ∧ U ≤ 1 ∧ 0 ≤ V ∧ V ≤ 1 ∧
      Sendov911Capstone.Box.poly box_54 U V ≤ 0 := by
  have ha0 : (0:ℝ) < a := by linarith
  have ha1 : a ≤ 1 := by linarith
  have hsig : sig ≤ (27422806098205692817843150524782641/3758376591360000000000000000000000 : ℝ) := by
    rw [hsigeq]
    exact Rows10.sigma_row_54 rr hrlo (fun k => le_trans (hrhi k) (by linarith)) hprod
  have hL := Rows10.L_row_54 h0 h1
  have hloc' : ((9:ℕ):ℝ) ≤ ((9:ℕ):ℝ) * a * mu.re + (1 - a ^ 2) * sig := by
    push_cast
    exact hloc
  have hkey := MidChainN.key_of_loc ha1 ha0.le hloc' hsig
  have hL' : 0 ≤ (27422806098205692817843150524782641/3758376591360000000000000000000000 : ℝ) * a ^ 2 - ((9:ℕ):ℝ) * (78831/100000 : ℝ) * a + ((9:ℕ):ℝ) - (27422806098205692817843150524782641/3758376591360000000000000000000000 : ℝ) := by
    push_cast
    exact hL
  have hlamu := MidChainN.lam_le_u (by norm_num) ha0 hkey hL'
  have hmunorm := MidChainN.lam_le_norm mu hlamu
  have hmu0 : mu ≠ 0 := by
    intro hcon
    rw [hcon] at hmunorm
    simp only [norm_zero] at hmunorm
    norm_num at hmunorm
  have hetale := MidChainN.eta_sq_le mu (by norm_num) hnormSq hmunorm
  have hetaY : eta ≤ (7691/12500 : ℝ) :=
    MidChainN.eta_le_Y (by norm_num) heta0 hetale Rows10.Y_row_54
  have hP := MidChainN.row_nonpos (N := 9) (alpha := (27/50 : ℝ)) (beta := (11/20 : ℝ))
    (S := (27422806098205692817843150524782641/3758376591360000000000000000000000 : ℝ)) (lam := (78831/100000 : ℝ)) (sigma := sig) (by norm_num)
    CTab10.c10 (fun m _ => CTab10.c10_nonneg m) mu Df a eta
    (by norm_num) (by norm_num) (by norm_num) Rows10.lam_row_54 h0 h1
    heta0 hnormSq hloc' hsig hL' hz
    (CTab10.cTable10 Df eta heta0 hz hD2)
    (by push_cast; norm_num; exact JBoundN.J_closed_form_deg10 hmu0 a)
    (by push_cast; norm_num; exact hI)
  refine ⟨100 * (a - (27/50 : ℝ)), eta / (7691/12500 : ℝ), by linarith, by linarith,
    div_nonneg heta0 (by norm_num), (div_le_one (by norm_num)).mpr hetaY, ?_⟩
  have heq := eq_54 (100 * (a - (27/50 : ℝ))) (eta / (7691/12500 : ℝ))
  rw [show (27/50 : ℝ) + (1/100) * (100 * (a - (27/50 : ℝ))) = a by ring] at heq
  rw [show (7691/12500 : ℝ) * (eta / (7691/12500 : ℝ)) = eta by field_simp <;> ring] at heq
  exact le_of_eq_of_le heq.symm hP

/-! ### Row 55: `a ∈ [11/20, 14/25]`, S = 111590304084706774021315921217891369/15028090216219425201416015625000000, λ = 38593/50000, Y = 3179/5000 -/

noncomputable def box_55 : Box where
  nx := 10
  ny := 10
  A := Sendov911Box10_55.tab Sendov911Box10_55.Adata
  D := (Sendov911Box10_55.Dscale : ℝ)
  hD := by norm_num [Sendov911Box10_55.Dscale]

theorem eq_55 (U V : ℝ) :
    (1 - ((11/20 : ℝ) + (1/100) * U)) / (((9:ℕ):ℝ) + 1) + ((3179/5000 : ℝ) * V) ^ 2 / (2 * (((9:ℕ):ℝ) + 1))
      - MidChainN.hh 9 (111590304084706774021315921217891369/15028090216219425201416015625000000 : ℝ) ((11/20 : ℝ) + (1/100) * U) ((3179/5000 : ℝ) * V) ^ 5 / ((((9:ℕ):ℝ) + 1) * (38593/50000 : ℝ))
      - MidChainN.Emaj 9 CTab10.c10 (111590304084706774021315921217891369/15028090216219425201416015625000000 : ℝ) ((11/20 : ℝ) + (1/100) * U) ((3179/5000 : ℝ) * V)
    = ∑ k ∈ Finset.range 11, ∑ l ∈ Finset.range 11,
        ((Sendov911Box10_55.tab Sendov911Box10_55.Adata k l : ℝ)
          / (Sendov911Box10_55.Dscale : ℝ)) * U ^ k * V ^ l := by
  rw [EmajEq.Emaj10_eq]
  unfold MidChainN.hh
  push_cast
  simp only [Finset.sum_range_succ, Finset.sum_range_zero,
    Sendov911Box10_55.tab, Sendov911Box10_55.Adata, Sendov911Box10_55.Dscale,
    List.getD_cons_zero, List.getD_cons_succ, zero_add]
  norm_num
  ring

theorem red_55 (mu : ℂ) (Df : Fin 9 → ℂ) (rr : Fin 9 → ℝ) (a eta sig : ℝ)
    (h0 : (11/20 : ℝ) ≤ a) (h1 : a ≤ (14/25 : ℝ))
    (heta0 : 0 ≤ eta) (hnormSq : Complex.normSq mu = 1 - eta ^ 2)
    (hloc : (9:ℝ) ≤ 9 * a * mu.re + (1 - a ^ 2) * sig)
    (hsigeq : sig = ∑ k, 1 / rr k ^ 2)
    (hrlo : ∀ k, (309/500 : ℝ) ≤ rr k) (hrhi : ∀ k, rr k ≤ a + 1)
    (hprod : (10:ℝ) ≤ ∏ k, rr k)
    (hz : ∑ j, Df j = 0)
    (hD2 : ∑ j, ‖Df j‖ ^ 2 ≤ 9 * eta ^ 2)
    (hI : ‖∫ t in (0:ℝ)..a, ∏ j, ((1:ℂ) - (t:ℂ) * (mu + Df j))‖ < a / 10) :
    ∃ U V : ℝ, 0 ≤ U ∧ U ≤ 1 ∧ 0 ≤ V ∧ V ≤ 1 ∧
      Sendov911Capstone.Box.poly box_55 U V ≤ 0 := by
  have ha0 : (0:ℝ) < a := by linarith
  have ha1 : a ≤ 1 := by linarith
  have hsig : sig ≤ (111590304084706774021315921217891369/15028090216219425201416015625000000 : ℝ) := by
    rw [hsigeq]
    exact Rows10.sigma_row_55 rr hrlo (fun k => le_trans (hrhi k) (by linarith)) hprod
  have hL := Rows10.L_row_55 h0 h1
  have hloc' : ((9:ℕ):ℝ) ≤ ((9:ℕ):ℝ) * a * mu.re + (1 - a ^ 2) * sig := by
    push_cast
    exact hloc
  have hkey := MidChainN.key_of_loc ha1 ha0.le hloc' hsig
  have hL' : 0 ≤ (111590304084706774021315921217891369/15028090216219425201416015625000000 : ℝ) * a ^ 2 - ((9:ℕ):ℝ) * (38593/50000 : ℝ) * a + ((9:ℕ):ℝ) - (111590304084706774021315921217891369/15028090216219425201416015625000000 : ℝ) := by
    push_cast
    exact hL
  have hlamu := MidChainN.lam_le_u (by norm_num) ha0 hkey hL'
  have hmunorm := MidChainN.lam_le_norm mu hlamu
  have hmu0 : mu ≠ 0 := by
    intro hcon
    rw [hcon] at hmunorm
    simp only [norm_zero] at hmunorm
    norm_num at hmunorm
  have hetale := MidChainN.eta_sq_le mu (by norm_num) hnormSq hmunorm
  have hetaY : eta ≤ (3179/5000 : ℝ) :=
    MidChainN.eta_le_Y (by norm_num) heta0 hetale Rows10.Y_row_55
  have hP := MidChainN.row_nonpos (N := 9) (alpha := (11/20 : ℝ)) (beta := (14/25 : ℝ))
    (S := (111590304084706774021315921217891369/15028090216219425201416015625000000 : ℝ)) (lam := (38593/50000 : ℝ)) (sigma := sig) (by norm_num)
    CTab10.c10 (fun m _ => CTab10.c10_nonneg m) mu Df a eta
    (by norm_num) (by norm_num) (by norm_num) Rows10.lam_row_55 h0 h1
    heta0 hnormSq hloc' hsig hL' hz
    (CTab10.cTable10 Df eta heta0 hz hD2)
    (by push_cast; norm_num; exact JBoundN.J_closed_form_deg10 hmu0 a)
    (by push_cast; norm_num; exact hI)
  refine ⟨100 * (a - (11/20 : ℝ)), eta / (3179/5000 : ℝ), by linarith, by linarith,
    div_nonneg heta0 (by norm_num), (div_le_one (by norm_num)).mpr hetaY, ?_⟩
  have heq := eq_55 (100 * (a - (11/20 : ℝ))) (eta / (3179/5000 : ℝ))
  rw [show (11/20 : ℝ) + (1/100) * (100 * (a - (11/20 : ℝ))) = a by ring] at heq
  rw [show (3179/5000 : ℝ) * (eta / (3179/5000 : ℝ)) = eta by field_simp <;> ring] at heq
  exact le_of_eq_of_le heq.symm hP

/-! ### Row 56: `a ∈ [14/25, 57/100]`, S = 4453785352081383287862612259536501719460962161/588377792250000000000000000000000000000000000, λ = 1887/2500, Y = 16399/25000 -/

noncomputable def box_56 : Box where
  nx := 10
  ny := 10
  A := Sendov911Box10_56.tab Sendov911Box10_56.Adata
  D := (Sendov911Box10_56.Dscale : ℝ)
  hD := by norm_num [Sendov911Box10_56.Dscale]

theorem eq_56 (U V : ℝ) :
    (1 - ((14/25 : ℝ) + (1/100) * U)) / (((9:ℕ):ℝ) + 1) + ((16399/25000 : ℝ) * V) ^ 2 / (2 * (((9:ℕ):ℝ) + 1))
      - MidChainN.hh 9 (4453785352081383287862612259536501719460962161/588377792250000000000000000000000000000000000 : ℝ) ((14/25 : ℝ) + (1/100) * U) ((16399/25000 : ℝ) * V) ^ 5 / ((((9:ℕ):ℝ) + 1) * (1887/2500 : ℝ))
      - MidChainN.Emaj 9 CTab10.c10 (4453785352081383287862612259536501719460962161/588377792250000000000000000000000000000000000 : ℝ) ((14/25 : ℝ) + (1/100) * U) ((16399/25000 : ℝ) * V)
    = ∑ k ∈ Finset.range 11, ∑ l ∈ Finset.range 11,
        ((Sendov911Box10_56.tab Sendov911Box10_56.Adata k l : ℝ)
          / (Sendov911Box10_56.Dscale : ℝ)) * U ^ k * V ^ l := by
  rw [EmajEq.Emaj10_eq]
  unfold MidChainN.hh
  push_cast
  simp only [Finset.sum_range_succ, Finset.sum_range_zero,
    Sendov911Box10_56.tab, Sendov911Box10_56.Adata, Sendov911Box10_56.Dscale,
    List.getD_cons_zero, List.getD_cons_succ, zero_add]
  norm_num
  ring

theorem red_56 (mu : ℂ) (Df : Fin 9 → ℂ) (rr : Fin 9 → ℝ) (a eta sig : ℝ)
    (h0 : (14/25 : ℝ) ≤ a) (h1 : a ≤ (57/100 : ℝ))
    (heta0 : 0 ≤ eta) (hnormSq : Complex.normSq mu = 1 - eta ^ 2)
    (hloc : (9:ℝ) ≤ 9 * a * mu.re + (1 - a ^ 2) * sig)
    (hsigeq : sig = ∑ k, 1 / rr k ^ 2)
    (hrlo : ∀ k, (309/500 : ℝ) ≤ rr k) (hrhi : ∀ k, rr k ≤ a + 1)
    (hprod : (10:ℝ) ≤ ∏ k, rr k)
    (hz : ∑ j, Df j = 0)
    (hD2 : ∑ j, ‖Df j‖ ^ 2 ≤ 9 * eta ^ 2)
    (hI : ‖∫ t in (0:ℝ)..a, ∏ j, ((1:ℂ) - (t:ℂ) * (mu + Df j))‖ < a / 10) :
    ∃ U V : ℝ, 0 ≤ U ∧ U ≤ 1 ∧ 0 ≤ V ∧ V ≤ 1 ∧
      Sendov911Capstone.Box.poly box_56 U V ≤ 0 := by
  have ha0 : (0:ℝ) < a := by linarith
  have ha1 : a ≤ 1 := by linarith
  have hsig : sig ≤ (4453785352081383287862612259536501719460962161/588377792250000000000000000000000000000000000 : ℝ) := by
    rw [hsigeq]
    exact Rows10.sigma_row_56 rr hrlo (fun k => le_trans (hrhi k) (by linarith)) hprod
  have hL := Rows10.L_row_56 h0 h1
  have hloc' : ((9:ℕ):ℝ) ≤ ((9:ℕ):ℝ) * a * mu.re + (1 - a ^ 2) * sig := by
    push_cast
    exact hloc
  have hkey := MidChainN.key_of_loc ha1 ha0.le hloc' hsig
  have hL' : 0 ≤ (4453785352081383287862612259536501719460962161/588377792250000000000000000000000000000000000 : ℝ) * a ^ 2 - ((9:ℕ):ℝ) * (1887/2500 : ℝ) * a + ((9:ℕ):ℝ) - (4453785352081383287862612259536501719460962161/588377792250000000000000000000000000000000000 : ℝ) := by
    push_cast
    exact hL
  have hlamu := MidChainN.lam_le_u (by norm_num) ha0 hkey hL'
  have hmunorm := MidChainN.lam_le_norm mu hlamu
  have hmu0 : mu ≠ 0 := by
    intro hcon
    rw [hcon] at hmunorm
    simp only [norm_zero] at hmunorm
    norm_num at hmunorm
  have hetale := MidChainN.eta_sq_le mu (by norm_num) hnormSq hmunorm
  have hetaY : eta ≤ (16399/25000 : ℝ) :=
    MidChainN.eta_le_Y (by norm_num) heta0 hetale Rows10.Y_row_56
  have hP := MidChainN.row_nonpos (N := 9) (alpha := (14/25 : ℝ)) (beta := (57/100 : ℝ))
    (S := (4453785352081383287862612259536501719460962161/588377792250000000000000000000000000000000000 : ℝ)) (lam := (1887/2500 : ℝ)) (sigma := sig) (by norm_num)
    CTab10.c10 (fun m _ => CTab10.c10_nonneg m) mu Df a eta
    (by norm_num) (by norm_num) (by norm_num) Rows10.lam_row_56 h0 h1
    heta0 hnormSq hloc' hsig hL' hz
    (CTab10.cTable10 Df eta heta0 hz hD2)
    (by push_cast; norm_num; exact JBoundN.J_closed_form_deg10 hmu0 a)
    (by push_cast; norm_num; exact hI)
  refine ⟨100 * (a - (14/25 : ℝ)), eta / (16399/25000 : ℝ), by linarith, by linarith,
    div_nonneg heta0 (by norm_num), (div_le_one (by norm_num)).mpr hetaY, ?_⟩
  have heq := eq_56 (100 * (a - (14/25 : ℝ))) (eta / (16399/25000 : ℝ))
  rw [show (14/25 : ℝ) + (1/100) * (100 * (a - (14/25 : ℝ))) = a by ring] at heq
  rw [show (16399/25000 : ℝ) * (eta / (16399/25000 : ℝ)) = eta by field_simp <;> ring] at heq
  exact le_of_eq_of_le heq.symm hP

/-! ### Row 57: `a ∈ [57/100, 29/50]`, S = 70286693534708663736675617906476305407281/9092665420532226562500000000000000000000, λ = 4607/6250, Y = 67577/100000 -/

noncomputable def box_57 : Box where
  nx := 10
  ny := 10
  A := Sendov911Box10_57.tab Sendov911Box10_57.Adata
  D := (Sendov911Box10_57.Dscale : ℝ)
  hD := by norm_num [Sendov911Box10_57.Dscale]

theorem eq_57 (U V : ℝ) :
    (1 - ((57/100 : ℝ) + (1/100) * U)) / (((9:ℕ):ℝ) + 1) + ((67577/100000 : ℝ) * V) ^ 2 / (2 * (((9:ℕ):ℝ) + 1))
      - MidChainN.hh 9 (70286693534708663736675617906476305407281/9092665420532226562500000000000000000000 : ℝ) ((57/100 : ℝ) + (1/100) * U) ((67577/100000 : ℝ) * V) ^ 5 / ((((9:ℕ):ℝ) + 1) * (4607/6250 : ℝ))
      - MidChainN.Emaj 9 CTab10.c10 (70286693534708663736675617906476305407281/9092665420532226562500000000000000000000 : ℝ) ((57/100 : ℝ) + (1/100) * U) ((67577/100000 : ℝ) * V)
    = ∑ k ∈ Finset.range 11, ∑ l ∈ Finset.range 11,
        ((Sendov911Box10_57.tab Sendov911Box10_57.Adata k l : ℝ)
          / (Sendov911Box10_57.Dscale : ℝ)) * U ^ k * V ^ l := by
  rw [EmajEq.Emaj10_eq]
  unfold MidChainN.hh
  push_cast
  simp only [Finset.sum_range_succ, Finset.sum_range_zero,
    Sendov911Box10_57.tab, Sendov911Box10_57.Adata, Sendov911Box10_57.Dscale,
    List.getD_cons_zero, List.getD_cons_succ, zero_add]
  norm_num
  ring

theorem red_57 (mu : ℂ) (Df : Fin 9 → ℂ) (rr : Fin 9 → ℝ) (a eta sig : ℝ)
    (h0 : (57/100 : ℝ) ≤ a) (h1 : a ≤ (29/50 : ℝ))
    (heta0 : 0 ≤ eta) (hnormSq : Complex.normSq mu = 1 - eta ^ 2)
    (hloc : (9:ℝ) ≤ 9 * a * mu.re + (1 - a ^ 2) * sig)
    (hsigeq : sig = ∑ k, 1 / rr k ^ 2)
    (hrlo : ∀ k, (309/500 : ℝ) ≤ rr k) (hrhi : ∀ k, rr k ≤ a + 1)
    (hprod : (10:ℝ) ≤ ∏ k, rr k)
    (hz : ∑ j, Df j = 0)
    (hD2 : ∑ j, ‖Df j‖ ^ 2 ≤ 9 * eta ^ 2)
    (hI : ‖∫ t in (0:ℝ)..a, ∏ j, ((1:ℂ) - (t:ℂ) * (mu + Df j))‖ < a / 10) :
    ∃ U V : ℝ, 0 ≤ U ∧ U ≤ 1 ∧ 0 ≤ V ∧ V ≤ 1 ∧
      Sendov911Capstone.Box.poly box_57 U V ≤ 0 := by
  have ha0 : (0:ℝ) < a := by linarith
  have ha1 : a ≤ 1 := by linarith
  have hsig : sig ≤ (70286693534708663736675617906476305407281/9092665420532226562500000000000000000000 : ℝ) := by
    rw [hsigeq]
    exact Rows10.sigma_row_57 rr hrlo (fun k => le_trans (hrhi k) (by linarith)) hprod
  have hL := Rows10.L_row_57 h0 h1
  have hloc' : ((9:ℕ):ℝ) ≤ ((9:ℕ):ℝ) * a * mu.re + (1 - a ^ 2) * sig := by
    push_cast
    exact hloc
  have hkey := MidChainN.key_of_loc ha1 ha0.le hloc' hsig
  have hL' : 0 ≤ (70286693534708663736675617906476305407281/9092665420532226562500000000000000000000 : ℝ) * a ^ 2 - ((9:ℕ):ℝ) * (4607/6250 : ℝ) * a + ((9:ℕ):ℝ) - (70286693534708663736675617906476305407281/9092665420532226562500000000000000000000 : ℝ) := by
    push_cast
    exact hL
  have hlamu := MidChainN.lam_le_u (by norm_num) ha0 hkey hL'
  have hmunorm := MidChainN.lam_le_norm mu hlamu
  have hmu0 : mu ≠ 0 := by
    intro hcon
    rw [hcon] at hmunorm
    simp only [norm_zero] at hmunorm
    norm_num at hmunorm
  have hetale := MidChainN.eta_sq_le mu (by norm_num) hnormSq hmunorm
  have hetaY : eta ≤ (67577/100000 : ℝ) :=
    MidChainN.eta_le_Y (by norm_num) heta0 hetale Rows10.Y_row_57
  have hP := MidChainN.row_nonpos (N := 9) (alpha := (57/100 : ℝ)) (beta := (29/50 : ℝ))
    (S := (70286693534708663736675617906476305407281/9092665420532226562500000000000000000000 : ℝ)) (lam := (4607/6250 : ℝ)) (sigma := sig) (by norm_num)
    CTab10.c10 (fun m _ => CTab10.c10_nonneg m) mu Df a eta
    (by norm_num) (by norm_num) (by norm_num) Rows10.lam_row_57 h0 h1
    heta0 hnormSq hloc' hsig hL' hz
    (CTab10.cTable10 Df eta heta0 hz hD2)
    (by push_cast; norm_num; exact JBoundN.J_closed_form_deg10 hmu0 a)
    (by push_cast; norm_num; exact hI)
  refine ⟨100 * (a - (57/100 : ℝ)), eta / (67577/100000 : ℝ), by linarith, by linarith,
    div_nonneg heta0 (by norm_num), (div_le_one (by norm_num)).mpr hetaY, ?_⟩
  have heq := eq_57 (100 * (a - (57/100 : ℝ))) (eta / (67577/100000 : ℝ))
  rw [show (57/100 : ℝ) + (1/100) * (100 * (a - (57/100 : ℝ))) = a by ring] at heq
  rw [show (67577/100000 : ℝ) * (eta / (67577/100000 : ℝ)) = eta by field_simp <;> ring] at heq
  exact le_of_eq_of_le heq.symm hP

/-! ### Row 58: `a ∈ [29/50, 59/100]`, S = 530242841066219607063601811274007834441107689/67051532250000000000000000000000000000000000, λ = 35941/50000, Y = 869/1250 -/

noncomputable def box_58 : Box where
  nx := 10
  ny := 10
  A := Sendov911Box10_58.tab Sendov911Box10_58.Adata
  D := (Sendov911Box10_58.Dscale : ℝ)
  hD := by norm_num [Sendov911Box10_58.Dscale]

theorem eq_58 (U V : ℝ) :
    (1 - ((29/50 : ℝ) + (1/100) * U)) / (((9:ℕ):ℝ) + 1) + ((869/1250 : ℝ) * V) ^ 2 / (2 * (((9:ℕ):ℝ) + 1))
      - MidChainN.hh 9 (530242841066219607063601811274007834441107689/67051532250000000000000000000000000000000000 : ℝ) ((29/50 : ℝ) + (1/100) * U) ((869/1250 : ℝ) * V) ^ 5 / ((((9:ℕ):ℝ) + 1) * (35941/50000 : ℝ))
      - MidChainN.Emaj 9 CTab10.c10 (530242841066219607063601811274007834441107689/67051532250000000000000000000000000000000000 : ℝ) ((29/50 : ℝ) + (1/100) * U) ((869/1250 : ℝ) * V)
    = ∑ k ∈ Finset.range 11, ∑ l ∈ Finset.range 11,
        ((Sendov911Box10_58.tab Sendov911Box10_58.Adata k l : ℝ)
          / (Sendov911Box10_58.Dscale : ℝ)) * U ^ k * V ^ l := by
  rw [EmajEq.Emaj10_eq]
  unfold MidChainN.hh
  push_cast
  simp only [Finset.sum_range_succ, Finset.sum_range_zero,
    Sendov911Box10_58.tab, Sendov911Box10_58.Adata, Sendov911Box10_58.Dscale,
    List.getD_cons_zero, List.getD_cons_succ, zero_add]
  norm_num
  ring

theorem red_58 (mu : ℂ) (Df : Fin 9 → ℂ) (rr : Fin 9 → ℝ) (a eta sig : ℝ)
    (h0 : (29/50 : ℝ) ≤ a) (h1 : a ≤ (59/100 : ℝ))
    (heta0 : 0 ≤ eta) (hnormSq : Complex.normSq mu = 1 - eta ^ 2)
    (hloc : (9:ℝ) ≤ 9 * a * mu.re + (1 - a ^ 2) * sig)
    (hsigeq : sig = ∑ k, 1 / rr k ^ 2)
    (hrlo : ∀ k, (309/500 : ℝ) ≤ rr k) (hrhi : ∀ k, rr k ≤ a + 1)
    (hprod : (10:ℝ) ≤ ∏ k, rr k)
    (hz : ∑ j, Df j = 0)
    (hD2 : ∑ j, ‖Df j‖ ^ 2 ≤ 9 * eta ^ 2)
    (hI : ‖∫ t in (0:ℝ)..a, ∏ j, ((1:ℂ) - (t:ℂ) * (mu + Df j))‖ < a / 10) :
    ∃ U V : ℝ, 0 ≤ U ∧ U ≤ 1 ∧ 0 ≤ V ∧ V ≤ 1 ∧
      Sendov911Capstone.Box.poly box_58 U V ≤ 0 := by
  have ha0 : (0:ℝ) < a := by linarith
  have ha1 : a ≤ 1 := by linarith
  have hsig : sig ≤ (530242841066219607063601811274007834441107689/67051532250000000000000000000000000000000000 : ℝ) := by
    rw [hsigeq]
    exact Rows10.sigma_row_58 rr hrlo (fun k => le_trans (hrhi k) (by linarith)) hprod
  have hL := Rows10.L_row_58 h0 h1
  have hloc' : ((9:ℕ):ℝ) ≤ ((9:ℕ):ℝ) * a * mu.re + (1 - a ^ 2) * sig := by
    push_cast
    exact hloc
  have hkey := MidChainN.key_of_loc ha1 ha0.le hloc' hsig
  have hL' : 0 ≤ (530242841066219607063601811274007834441107689/67051532250000000000000000000000000000000000 : ℝ) * a ^ 2 - ((9:ℕ):ℝ) * (35941/50000 : ℝ) * a + ((9:ℕ):ℝ) - (530242841066219607063601811274007834441107689/67051532250000000000000000000000000000000000 : ℝ) := by
    push_cast
    exact hL
  have hlamu := MidChainN.lam_le_u (by norm_num) ha0 hkey hL'
  have hmunorm := MidChainN.lam_le_norm mu hlamu
  have hmu0 : mu ≠ 0 := by
    intro hcon
    rw [hcon] at hmunorm
    simp only [norm_zero] at hmunorm
    norm_num at hmunorm
  have hetale := MidChainN.eta_sq_le mu (by norm_num) hnormSq hmunorm
  have hetaY : eta ≤ (869/1250 : ℝ) :=
    MidChainN.eta_le_Y (by norm_num) heta0 hetale Rows10.Y_row_58
  have hP := MidChainN.row_nonpos (N := 9) (alpha := (29/50 : ℝ)) (beta := (59/100 : ℝ))
    (S := (530242841066219607063601811274007834441107689/67051532250000000000000000000000000000000000 : ℝ)) (lam := (35941/50000 : ℝ)) (sigma := sig) (by norm_num)
    CTab10.c10 (fun m _ => CTab10.c10_nonneg m) mu Df a eta
    (by norm_num) (by norm_num) (by norm_num) Rows10.lam_row_58 h0 h1
    heta0 hnormSq hloc' hsig hL' hz
    (CTab10.cTable10 Df eta heta0 hz hD2)
    (by push_cast; norm_num; exact JBoundN.J_closed_form_deg10 hmu0 a)
    (by push_cast; norm_num; exact hI)
  refine ⟨100 * (a - (29/50 : ℝ)), eta / (869/1250 : ℝ), by linarith, by linarith,
    div_nonneg heta0 (by norm_num), (div_le_one (by norm_num)).mpr hetaY, ?_⟩
  have heq := eq_58 (100 * (a - (29/50 : ℝ))) (eta / (869/1250 : ℝ))
  rw [show (29/50 : ℝ) + (1/100) * (100 * (a - (29/50 : ℝ))) = a by ring] at heq
  rw [show (869/1250 : ℝ) * (eta / (869/1250 : ℝ)) = eta by field_simp <;> ring] at heq
  exact le_of_eq_of_le heq.symm hP

/-! ### Row 59: `a ∈ [59/100, 3/5]`, S = 36381998672604456123577243/4552888870239257812500000, λ = 71387/100000, Y = 17507/25000 -/

noncomputable def box_59 : Box where
  nx := 10
  ny := 10
  A := Sendov911Box10_59.tab Sendov911Box10_59.Adata
  D := (Sendov911Box10_59.Dscale : ℝ)
  hD := by norm_num [Sendov911Box10_59.Dscale]

theorem eq_59 (U V : ℝ) :
    (1 - ((59/100 : ℝ) + (1/100) * U)) / (((9:ℕ):ℝ) + 1) + ((17507/25000 : ℝ) * V) ^ 2 / (2 * (((9:ℕ):ℝ) + 1))
      - MidChainN.hh 9 (36381998672604456123577243/4552888870239257812500000 : ℝ) ((59/100 : ℝ) + (1/100) * U) ((17507/25000 : ℝ) * V) ^ 5 / ((((9:ℕ):ℝ) + 1) * (71387/100000 : ℝ))
      - MidChainN.Emaj 9 CTab10.c10 (36381998672604456123577243/4552888870239257812500000 : ℝ) ((59/100 : ℝ) + (1/100) * U) ((17507/25000 : ℝ) * V)
    = ∑ k ∈ Finset.range 11, ∑ l ∈ Finset.range 11,
        ((Sendov911Box10_59.tab Sendov911Box10_59.Adata k l : ℝ)
          / (Sendov911Box10_59.Dscale : ℝ)) * U ^ k * V ^ l := by
  rw [EmajEq.Emaj10_eq]
  unfold MidChainN.hh
  push_cast
  simp only [Finset.sum_range_succ, Finset.sum_range_zero,
    Sendov911Box10_59.tab, Sendov911Box10_59.Adata, Sendov911Box10_59.Dscale,
    List.getD_cons_zero, List.getD_cons_succ, zero_add]
  norm_num
  ring

theorem red_59 (mu : ℂ) (Df : Fin 9 → ℂ) (rr : Fin 9 → ℝ) (a eta sig : ℝ)
    (h0 : (59/100 : ℝ) ≤ a) (h1 : a ≤ (3/5 : ℝ))
    (heta0 : 0 ≤ eta) (hnormSq : Complex.normSq mu = 1 - eta ^ 2)
    (hloc : (9:ℝ) ≤ 9 * a * mu.re + (1 - a ^ 2) * sig)
    (hsigeq : sig = ∑ k, 1 / rr k ^ 2)
    (hrlo : ∀ k, (309/500 : ℝ) ≤ rr k) (hrhi : ∀ k, rr k ≤ a + 1)
    (hprod : (10:ℝ) ≤ ∏ k, rr k)
    (hz : ∑ j, Df j = 0)
    (hD2 : ∑ j, ‖Df j‖ ^ 2 ≤ 9 * eta ^ 2)
    (hI : ‖∫ t in (0:ℝ)..a, ∏ j, ((1:ℂ) - (t:ℂ) * (mu + Df j))‖ < a / 10) :
    ∃ U V : ℝ, 0 ≤ U ∧ U ≤ 1 ∧ 0 ≤ V ∧ V ≤ 1 ∧
      Sendov911Capstone.Box.poly box_59 U V ≤ 0 := by
  have ha0 : (0:ℝ) < a := by linarith
  have ha1 : a ≤ 1 := by linarith
  have hsig : sig ≤ (36381998672604456123577243/4552888870239257812500000 : ℝ) := by
    rw [hsigeq]
    exact Rows10.sigma_row_59 rr hrlo (fun k => le_trans (hrhi k) (by linarith)) hprod
  have hL := Rows10.L_row_59 h0 h1
  have hloc' : ((9:ℕ):ℝ) ≤ ((9:ℕ):ℝ) * a * mu.re + (1 - a ^ 2) * sig := by
    push_cast
    exact hloc
  have hkey := MidChainN.key_of_loc ha1 ha0.le hloc' hsig
  have hL' : 0 ≤ (36381998672604456123577243/4552888870239257812500000 : ℝ) * a ^ 2 - ((9:ℕ):ℝ) * (71387/100000 : ℝ) * a + ((9:ℕ):ℝ) - (36381998672604456123577243/4552888870239257812500000 : ℝ) := by
    push_cast
    exact hL
  have hlamu := MidChainN.lam_le_u (by norm_num) ha0 hkey hL'
  have hmunorm := MidChainN.lam_le_norm mu hlamu
  have hmu0 : mu ≠ 0 := by
    intro hcon
    rw [hcon] at hmunorm
    simp only [norm_zero] at hmunorm
    norm_num at hmunorm
  have hetale := MidChainN.eta_sq_le mu (by norm_num) hnormSq hmunorm
  have hetaY : eta ≤ (17507/25000 : ℝ) :=
    MidChainN.eta_le_Y (by norm_num) heta0 hetale Rows10.Y_row_59
  have hP := MidChainN.row_nonpos (N := 9) (alpha := (59/100 : ℝ)) (beta := (3/5 : ℝ))
    (S := (36381998672604456123577243/4552888870239257812500000 : ℝ)) (lam := (71387/100000 : ℝ)) (sigma := sig) (by norm_num)
    CTab10.c10 (fun m _ => CTab10.c10_nonneg m) mu Df a eta
    (by norm_num) (by norm_num) (by norm_num) Rows10.lam_row_59 h0 h1
    heta0 hnormSq hloc' hsig hL' hz
    (CTab10.cTable10 Df eta heta0 hz hD2)
    (by push_cast; norm_num; exact JBoundN.J_closed_form_deg10 hmu0 a)
    (by push_cast; norm_num; exact hI)
  refine ⟨100 * (a - (59/100 : ℝ)), eta / (17507/25000 : ℝ), by linarith, by linarith,
    div_nonneg heta0 (by norm_num), (div_le_one (by norm_num)).mpr hetaY, ?_⟩
  have heq := eq_59 (100 * (a - (59/100 : ℝ))) (eta / (17507/25000 : ℝ))
  rw [show (59/100 : ℝ) + (1/100) * (100 * (a - (59/100 : ℝ))) = a by ring] at heq
  rw [show (17507/25000 : ℝ) * (eta / (17507/25000 : ℝ)) = eta by field_simp <;> ring] at heq
  exact le_of_eq_of_le heq.symm hP

noncomputable def chunk : List Box :=
  [box_50, box_51, box_52, box_53, box_54, box_55, box_56, box_57, box_58, box_59]

theorem chunkPos : CoveringPositive chunk := by
  unfold chunk
  refine coveringPositive_cons (fun _ _ h1 h2 h3 h4 => Sendov911Box10_50.box_positive h1 h2 h3 h4)
    (coveringPositive_cons (fun _ _ h1 h2 h3 h4 => Sendov911Box10_51.box_positive h1 h2 h3 h4)
    (coveringPositive_cons (fun _ _ h1 h2 h3 h4 => Sendov911Box10_52.box_positive h1 h2 h3 h4)
    (coveringPositive_cons (fun _ _ h1 h2 h3 h4 => Sendov911Box10_53.box_positive h1 h2 h3 h4)
    (coveringPositive_cons (fun _ _ h1 h2 h3 h4 => Sendov911Box10_54.box_positive h1 h2 h3 h4)
    (coveringPositive_cons (fun _ _ h1 h2 h3 h4 => Sendov911Box10_55.box_positive h1 h2 h3 h4)
    (coveringPositive_cons (fun _ _ h1 h2 h3 h4 => Sendov911Box10_56.box_positive h1 h2 h3 h4)
    (coveringPositive_cons (fun _ _ h1 h2 h3 h4 => Sendov911Box10_57.box_positive h1 h2 h3 h4)
    (coveringPositive_cons (fun _ _ h1 h2 h3 h4 => Sendov911Box10_58.box_positive h1 h2 h3 h4)
    (coveringPositive_cons (fun _ _ h1 h2 h3 h4 => Sendov911Box10_59.box_positive h1 h2 h3 h4)
    (coveringPositive_nil))))))))))

end SendovN.Red10B

#print axioms SendovN.Red10B.eq_50
#print axioms SendovN.Red10B.red_50
#print axioms SendovN.Red10B.eq_51
#print axioms SendovN.Red10B.red_51
#print axioms SendovN.Red10B.eq_52
#print axioms SendovN.Red10B.red_52
#print axioms SendovN.Red10B.eq_53
#print axioms SendovN.Red10B.red_53
#print axioms SendovN.Red10B.eq_54
#print axioms SendovN.Red10B.red_54
#print axioms SendovN.Red10B.eq_55
#print axioms SendovN.Red10B.red_55
#print axioms SendovN.Red10B.eq_56
#print axioms SendovN.Red10B.red_56
#print axioms SendovN.Red10B.eq_57
#print axioms SendovN.Red10B.red_57
#print axioms SendovN.Red10B.eq_58
#print axioms SendovN.Red10B.red_58
#print axioms SendovN.Red10B.eq_59
#print axioms SendovN.Red10B.red_59
#print axioms SendovN.Red10B.chunkPos
