import Mathlib
import SendovNEmajEq
import SendovNRows10
import SendovNCoverGrid
import Sendov911Box10_70
import Sendov911Box10_71
import Sendov911Box10_72
import Sendov911Box10_73
import Sendov911Box10_74
import Sendov911Box10_75
import Sendov911Box10_76
import Sendov911Box10_77
import Sendov911Box10_78
import Sendov911Box10_79

set_option maxHeartbeats 4000000000
set_option maxRecDepth 100000

/-!
# D10-6/7 assembly chunk D (rows 70–79; generated by
emit_assembly1011.py, do not hand-edit)

Per row: the `Box` value, the hEq identity against the shipped monomial table
(regenerated from the verifier arithmetic and matched integer-for-integer
before emission), and the row reduction `red_i` (`row_nonpos` → pullback).
-/

namespace SendovN.Red10D

open Finset Sendov911Capstone

/-! ### Row 70: `a ∈ [7/10, 71/100]`, S = 5299794866527282517943399522033661510822207523/646287018750000000000000000000000000000000000, λ = 76473/100000, Y = 16109/25000 -/

noncomputable def box_70 : Box where
  nx := 10
  ny := 10
  A := Sendov911Box10_70.tab Sendov911Box10_70.Adata
  D := (Sendov911Box10_70.Dscale : ℝ)
  hD := by norm_num [Sendov911Box10_70.Dscale]

theorem eq_70 (U V : ℝ) :
    (1 - ((7/10 : ℝ) + (1/100) * U)) / (((9:ℕ):ℝ) + 1) + ((16109/25000 : ℝ) * V) ^ 2 / (2 * (((9:ℕ):ℝ) + 1))
      - MidChainN.hh 9 (5299794866527282517943399522033661510822207523/646287018750000000000000000000000000000000000 : ℝ) ((7/10 : ℝ) + (1/100) * U) ((16109/25000 : ℝ) * V) ^ 5 / ((((9:ℕ):ℝ) + 1) * (76473/100000 : ℝ))
      - MidChainN.Emaj 9 CTab10.c10 (5299794866527282517943399522033661510822207523/646287018750000000000000000000000000000000000 : ℝ) ((7/10 : ℝ) + (1/100) * U) ((16109/25000 : ℝ) * V)
    = ∑ k ∈ Finset.range 11, ∑ l ∈ Finset.range 11,
        ((Sendov911Box10_70.tab Sendov911Box10_70.Adata k l : ℝ)
          / (Sendov911Box10_70.Dscale : ℝ)) * U ^ k * V ^ l := by
  rw [EmajEq.Emaj10_eq]
  unfold MidChainN.hh
  push_cast
  simp only [Finset.sum_range_succ, Finset.sum_range_zero,
    Sendov911Box10_70.tab, Sendov911Box10_70.Adata, Sendov911Box10_70.Dscale,
    List.getD_cons_zero, List.getD_cons_succ, zero_add]
  norm_num
  ring

theorem red_70 (mu : ℂ) (Df : Fin 9 → ℂ) (rr : Fin 9 → ℝ) (a eta sig : ℝ)
    (h0 : (7/10 : ℝ) ≤ a) (h1 : a ≤ (71/100 : ℝ))
    (heta0 : 0 ≤ eta) (hnormSq : Complex.normSq mu = 1 - eta ^ 2)
    (hloc : (9:ℝ) ≤ 9 * a * mu.re + (1 - a ^ 2) * sig)
    (hsigeq : sig = ∑ k, 1 / rr k ^ 2)
    (hrlo : ∀ k, (309/500 : ℝ) ≤ rr k) (hrhi : ∀ k, rr k ≤ a + 1)
    (hprod : (10:ℝ) ≤ ∏ k, rr k)
    (hz : ∑ j, Df j = 0)
    (hD2 : ∑ j, ‖Df j‖ ^ 2 ≤ 9 * eta ^ 2)
    (hI : ‖∫ t in (0:ℝ)..a, ∏ j, ((1:ℂ) - (t:ℂ) * (mu + Df j))‖ < a / 10) :
    ∃ U V : ℝ, 0 ≤ U ∧ U ≤ 1 ∧ 0 ≤ V ∧ V ≤ 1 ∧
      Sendov911Capstone.Box.poly box_70 U V ≤ 0 := by
  have ha0 : (0:ℝ) < a := by linarith
  have ha1 : a ≤ 1 := by linarith
  have hsig : sig ≤ (5299794866527282517943399522033661510822207523/646287018750000000000000000000000000000000000 : ℝ) := by
    rw [hsigeq]
    exact Rows10.sigma_row_70 rr hrlo (fun k => le_trans (hrhi k) (by linarith)) hprod
  have hL := Rows10.L_row_70 h0 h1
  have hloc' : ((9:ℕ):ℝ) ≤ ((9:ℕ):ℝ) * a * mu.re + (1 - a ^ 2) * sig := by
    push_cast
    exact hloc
  have hkey := MidChainN.key_of_loc ha1 ha0.le hloc' hsig
  have hL' : 0 ≤ (5299794866527282517943399522033661510822207523/646287018750000000000000000000000000000000000 : ℝ) * a ^ 2 - ((9:ℕ):ℝ) * (76473/100000 : ℝ) * a + ((9:ℕ):ℝ) - (5299794866527282517943399522033661510822207523/646287018750000000000000000000000000000000000 : ℝ) := by
    push_cast
    exact hL
  have hlamu := MidChainN.lam_le_u (by norm_num) ha0 hkey hL'
  have hmunorm := MidChainN.lam_le_norm mu hlamu
  have hmu0 : mu ≠ 0 := by
    intro hcon
    rw [hcon] at hmunorm
    simp only [norm_zero] at hmunorm
    norm_num at hmunorm
  have hetale := MidChainN.eta_sq_le mu (by norm_num) hnormSq hmunorm
  have hetaY : eta ≤ (16109/25000 : ℝ) :=
    MidChainN.eta_le_Y (by norm_num) heta0 hetale Rows10.Y_row_70
  have hP := MidChainN.row_nonpos (N := 9) (alpha := (7/10 : ℝ)) (beta := (71/100 : ℝ))
    (S := (5299794866527282517943399522033661510822207523/646287018750000000000000000000000000000000000 : ℝ)) (lam := (76473/100000 : ℝ)) (sigma := sig) (by norm_num)
    CTab10.c10 (fun m _ => CTab10.c10_nonneg m) mu Df a eta
    (by norm_num) (by norm_num) (by norm_num) Rows10.lam_row_70 h0 h1
    heta0 hnormSq hloc' hsig hL' hz
    (CTab10.cTable10 Df eta heta0 hz hD2)
    (by push_cast; norm_num; exact JBoundN.J_closed_form_deg10 hmu0 a)
    (by push_cast; norm_num; exact hI)
  refine ⟨100 * (a - (7/10 : ℝ)), eta / (16109/25000 : ℝ), by linarith, by linarith,
    div_nonneg heta0 (by norm_num), (div_le_one (by norm_num)).mpr hetaY, ?_⟩
  have heq := eq_70 (100 * (a - (7/10 : ℝ))) (eta / (16109/25000 : ℝ))
  rw [show (7/10 : ℝ) + (1/100) * (100 * (a - (7/10 : ℝ))) = a by ring] at heq
  rw [show (16109/25000 : ℝ) * (eta / (16109/25000 : ℝ)) = eta by field_simp <;> ring] at heq
  exact le_of_eq_of_le heq.symm hP

/-! ### Row 71: `a ∈ [71/100, 18/25]`, S = 542103057082621186423790202668950533809/65767902508378028869628906250000000000, λ = 76877/100000, Y = 63953/100000 -/

noncomputable def box_71 : Box where
  nx := 10
  ny := 10
  A := Sendov911Box10_71.tab Sendov911Box10_71.Adata
  D := (Sendov911Box10_71.Dscale : ℝ)
  hD := by norm_num [Sendov911Box10_71.Dscale]

theorem eq_71 (U V : ℝ) :
    (1 - ((71/100 : ℝ) + (1/100) * U)) / (((9:ℕ):ℝ) + 1) + ((63953/100000 : ℝ) * V) ^ 2 / (2 * (((9:ℕ):ℝ) + 1))
      - MidChainN.hh 9 (542103057082621186423790202668950533809/65767902508378028869628906250000000000 : ℝ) ((71/100 : ℝ) + (1/100) * U) ((63953/100000 : ℝ) * V) ^ 5 / ((((9:ℕ):ℝ) + 1) * (76877/100000 : ℝ))
      - MidChainN.Emaj 9 CTab10.c10 (542103057082621186423790202668950533809/65767902508378028869628906250000000000 : ℝ) ((71/100 : ℝ) + (1/100) * U) ((63953/100000 : ℝ) * V)
    = ∑ k ∈ Finset.range 11, ∑ l ∈ Finset.range 11,
        ((Sendov911Box10_71.tab Sendov911Box10_71.Adata k l : ℝ)
          / (Sendov911Box10_71.Dscale : ℝ)) * U ^ k * V ^ l := by
  rw [EmajEq.Emaj10_eq]
  unfold MidChainN.hh
  push_cast
  simp only [Finset.sum_range_succ, Finset.sum_range_zero,
    Sendov911Box10_71.tab, Sendov911Box10_71.Adata, Sendov911Box10_71.Dscale,
    List.getD_cons_zero, List.getD_cons_succ, zero_add]
  norm_num
  ring

theorem red_71 (mu : ℂ) (Df : Fin 9 → ℂ) (rr : Fin 9 → ℝ) (a eta sig : ℝ)
    (h0 : (71/100 : ℝ) ≤ a) (h1 : a ≤ (18/25 : ℝ))
    (heta0 : 0 ≤ eta) (hnormSq : Complex.normSq mu = 1 - eta ^ 2)
    (hloc : (9:ℝ) ≤ 9 * a * mu.re + (1 - a ^ 2) * sig)
    (hsigeq : sig = ∑ k, 1 / rr k ^ 2)
    (hrlo : ∀ k, (309/500 : ℝ) ≤ rr k) (hrhi : ∀ k, rr k ≤ a + 1)
    (hprod : (10:ℝ) ≤ ∏ k, rr k)
    (hz : ∑ j, Df j = 0)
    (hD2 : ∑ j, ‖Df j‖ ^ 2 ≤ 9 * eta ^ 2)
    (hI : ‖∫ t in (0:ℝ)..a, ∏ j, ((1:ℂ) - (t:ℂ) * (mu + Df j))‖ < a / 10) :
    ∃ U V : ℝ, 0 ≤ U ∧ U ≤ 1 ∧ 0 ≤ V ∧ V ≤ 1 ∧
      Sendov911Capstone.Box.poly box_71 U V ≤ 0 := by
  have ha0 : (0:ℝ) < a := by linarith
  have ha1 : a ≤ 1 := by linarith
  have hsig : sig ≤ (542103057082621186423790202668950533809/65767902508378028869628906250000000000 : ℝ) := by
    rw [hsigeq]
    exact Rows10.sigma_row_71 rr hrlo (fun k => le_trans (hrhi k) (by linarith)) hprod
  have hL := Rows10.L_row_71 h0 h1
  have hloc' : ((9:ℕ):ℝ) ≤ ((9:ℕ):ℝ) * a * mu.re + (1 - a ^ 2) * sig := by
    push_cast
    exact hloc
  have hkey := MidChainN.key_of_loc ha1 ha0.le hloc' hsig
  have hL' : 0 ≤ (542103057082621186423790202668950533809/65767902508378028869628906250000000000 : ℝ) * a ^ 2 - ((9:ℕ):ℝ) * (76877/100000 : ℝ) * a + ((9:ℕ):ℝ) - (542103057082621186423790202668950533809/65767902508378028869628906250000000000 : ℝ) := by
    push_cast
    exact hL
  have hlamu := MidChainN.lam_le_u (by norm_num) ha0 hkey hL'
  have hmunorm := MidChainN.lam_le_norm mu hlamu
  have hmu0 : mu ≠ 0 := by
    intro hcon
    rw [hcon] at hmunorm
    simp only [norm_zero] at hmunorm
    norm_num at hmunorm
  have hetale := MidChainN.eta_sq_le mu (by norm_num) hnormSq hmunorm
  have hetaY : eta ≤ (63953/100000 : ℝ) :=
    MidChainN.eta_le_Y (by norm_num) heta0 hetale Rows10.Y_row_71
  have hP := MidChainN.row_nonpos (N := 9) (alpha := (71/100 : ℝ)) (beta := (18/25 : ℝ))
    (S := (542103057082621186423790202668950533809/65767902508378028869628906250000000000 : ℝ)) (lam := (76877/100000 : ℝ)) (sigma := sig) (by norm_num)
    CTab10.c10 (fun m _ => CTab10.c10_nonneg m) mu Df a eta
    (by norm_num) (by norm_num) (by norm_num) Rows10.lam_row_71 h0 h1
    heta0 hnormSq hloc' hsig hL' hz
    (CTab10.cTable10 Df eta heta0 hz hD2)
    (by push_cast; norm_num; exact JBoundN.J_closed_form_deg10 hmu0 a)
    (by push_cast; norm_num; exact hI)
  refine ⟨100 * (a - (71/100 : ℝ)), eta / (63953/100000 : ℝ), by linarith, by linarith,
    div_nonneg heta0 (by norm_num), (div_le_one (by norm_num)).mpr hetaY, ?_⟩
  have heq := eq_71 (100 * (a - (71/100 : ℝ))) (eta / (63953/100000 : ℝ))
  rw [show (71/100 : ℝ) + (1/100) * (100 * (a - (71/100 : ℝ))) = a by ring] at heq
  rw [show (63953/100000 : ℝ) * (eta / (63953/100000 : ℝ)) = eta by field_simp <;> ring] at heq
  exact le_of_eq_of_le heq.symm hP

/-! ### Row 72: `a ∈ [18/25, 73/100]`, S = 148057400317162613272336990122317281541722202369/17860317806250000000000000000000000000000000000, λ = 38639/50000, Y = 15867/25000 -/

noncomputable def box_72 : Box where
  nx := 10
  ny := 10
  A := Sendov911Box10_72.tab Sendov911Box10_72.Adata
  D := (Sendov911Box10_72.Dscale : ℝ)
  hD := by norm_num [Sendov911Box10_72.Dscale]

theorem eq_72 (U V : ℝ) :
    (1 - ((18/25 : ℝ) + (1/100) * U)) / (((9:ℕ):ℝ) + 1) + ((15867/25000 : ℝ) * V) ^ 2 / (2 * (((9:ℕ):ℝ) + 1))
      - MidChainN.hh 9 (148057400317162613272336990122317281541722202369/17860317806250000000000000000000000000000000000 : ℝ) ((18/25 : ℝ) + (1/100) * U) ((15867/25000 : ℝ) * V) ^ 5 / ((((9:ℕ):ℝ) + 1) * (38639/50000 : ℝ))
      - MidChainN.Emaj 9 CTab10.c10 (148057400317162613272336990122317281541722202369/17860317806250000000000000000000000000000000000 : ℝ) ((18/25 : ℝ) + (1/100) * U) ((15867/25000 : ℝ) * V)
    = ∑ k ∈ Finset.range 11, ∑ l ∈ Finset.range 11,
        ((Sendov911Box10_72.tab Sendov911Box10_72.Adata k l : ℝ)
          / (Sendov911Box10_72.Dscale : ℝ)) * U ^ k * V ^ l := by
  rw [EmajEq.Emaj10_eq]
  unfold MidChainN.hh
  push_cast
  simp only [Finset.sum_range_succ, Finset.sum_range_zero,
    Sendov911Box10_72.tab, Sendov911Box10_72.Adata, Sendov911Box10_72.Dscale,
    List.getD_cons_zero, List.getD_cons_succ, zero_add]
  norm_num
  ring

theorem red_72 (mu : ℂ) (Df : Fin 9 → ℂ) (rr : Fin 9 → ℝ) (a eta sig : ℝ)
    (h0 : (18/25 : ℝ) ≤ a) (h1 : a ≤ (73/100 : ℝ))
    (heta0 : 0 ≤ eta) (hnormSq : Complex.normSq mu = 1 - eta ^ 2)
    (hloc : (9:ℝ) ≤ 9 * a * mu.re + (1 - a ^ 2) * sig)
    (hsigeq : sig = ∑ k, 1 / rr k ^ 2)
    (hrlo : ∀ k, (309/500 : ℝ) ≤ rr k) (hrhi : ∀ k, rr k ≤ a + 1)
    (hprod : (10:ℝ) ≤ ∏ k, rr k)
    (hz : ∑ j, Df j = 0)
    (hD2 : ∑ j, ‖Df j‖ ^ 2 ≤ 9 * eta ^ 2)
    (hI : ‖∫ t in (0:ℝ)..a, ∏ j, ((1:ℂ) - (t:ℂ) * (mu + Df j))‖ < a / 10) :
    ∃ U V : ℝ, 0 ≤ U ∧ U ≤ 1 ∧ 0 ≤ V ∧ V ≤ 1 ∧
      Sendov911Capstone.Box.poly box_72 U V ≤ 0 := by
  have ha0 : (0:ℝ) < a := by linarith
  have ha1 : a ≤ 1 := by linarith
  have hsig : sig ≤ (148057400317162613272336990122317281541722202369/17860317806250000000000000000000000000000000000 : ℝ) := by
    rw [hsigeq]
    exact Rows10.sigma_row_72 rr hrlo (fun k => le_trans (hrhi k) (by linarith)) hprod
  have hL := Rows10.L_row_72 h0 h1
  have hloc' : ((9:ℕ):ℝ) ≤ ((9:ℕ):ℝ) * a * mu.re + (1 - a ^ 2) * sig := by
    push_cast
    exact hloc
  have hkey := MidChainN.key_of_loc ha1 ha0.le hloc' hsig
  have hL' : 0 ≤ (148057400317162613272336990122317281541722202369/17860317806250000000000000000000000000000000000 : ℝ) * a ^ 2 - ((9:ℕ):ℝ) * (38639/50000 : ℝ) * a + ((9:ℕ):ℝ) - (148057400317162613272336990122317281541722202369/17860317806250000000000000000000000000000000000 : ℝ) := by
    push_cast
    exact hL
  have hlamu := MidChainN.lam_le_u (by norm_num) ha0 hkey hL'
  have hmunorm := MidChainN.lam_le_norm mu hlamu
  have hmu0 : mu ≠ 0 := by
    intro hcon
    rw [hcon] at hmunorm
    simp only [norm_zero] at hmunorm
    norm_num at hmunorm
  have hetale := MidChainN.eta_sq_le mu (by norm_num) hnormSq hmunorm
  have hetaY : eta ≤ (15867/25000 : ℝ) :=
    MidChainN.eta_le_Y (by norm_num) heta0 hetale Rows10.Y_row_72
  have hP := MidChainN.row_nonpos (N := 9) (alpha := (18/25 : ℝ)) (beta := (73/100 : ℝ))
    (S := (148057400317162613272336990122317281541722202369/17860317806250000000000000000000000000000000000 : ℝ)) (lam := (38639/50000 : ℝ)) (sigma := sig) (by norm_num)
    CTab10.c10 (fun m _ => CTab10.c10_nonneg m) mu Df a eta
    (by norm_num) (by norm_num) (by norm_num) Rows10.lam_row_72 h0 h1
    heta0 hnormSq hloc' hsig hL' hz
    (CTab10.cTable10 Df eta heta0 hz hD2)
    (by push_cast; norm_num; exact JBoundN.J_closed_form_deg10 hmu0 a)
    (by push_cast; norm_num; exact hI)
  refine ⟨100 * (a - (18/25 : ℝ)), eta / (15867/25000 : ℝ), by linarith, by linarith,
    div_nonneg heta0 (by norm_num), (div_le_one (by norm_num)).mpr hetaY, ?_⟩
  have heq := eq_72 (100 * (a - (18/25 : ℝ))) (eta / (15867/25000 : ℝ))
  rw [show (18/25 : ℝ) + (1/100) * (100 * (a - (18/25 : ℝ))) = a by ring] at heq
  rw [show (15867/25000 : ℝ) * (eta / (15867/25000 : ℝ)) = eta by field_simp <;> ring] at heq
  exact le_of_eq_of_le heq.symm hP

/-! ### Row 73: `a ∈ [73/100, 37/50]`, S = 1022103354851266015320143862892490866172561/122527345275878906250000000000000000000000, λ = 77679/100000, Y = 1968/3125 -/

noncomputable def box_73 : Box where
  nx := 10
  ny := 10
  A := Sendov911Box10_73.tab Sendov911Box10_73.Adata
  D := (Sendov911Box10_73.Dscale : ℝ)
  hD := by norm_num [Sendov911Box10_73.Dscale]

theorem eq_73 (U V : ℝ) :
    (1 - ((73/100 : ℝ) + (1/100) * U)) / (((9:ℕ):ℝ) + 1) + ((1968/3125 : ℝ) * V) ^ 2 / (2 * (((9:ℕ):ℝ) + 1))
      - MidChainN.hh 9 (1022103354851266015320143862892490866172561/122527345275878906250000000000000000000000 : ℝ) ((73/100 : ℝ) + (1/100) * U) ((1968/3125 : ℝ) * V) ^ 5 / ((((9:ℕ):ℝ) + 1) * (77679/100000 : ℝ))
      - MidChainN.Emaj 9 CTab10.c10 (1022103354851266015320143862892490866172561/122527345275878906250000000000000000000000 : ℝ) ((73/100 : ℝ) + (1/100) * U) ((1968/3125 : ℝ) * V)
    = ∑ k ∈ Finset.range 11, ∑ l ∈ Finset.range 11,
        ((Sendov911Box10_73.tab Sendov911Box10_73.Adata k l : ℝ)
          / (Sendov911Box10_73.Dscale : ℝ)) * U ^ k * V ^ l := by
  rw [EmajEq.Emaj10_eq]
  unfold MidChainN.hh
  push_cast
  simp only [Finset.sum_range_succ, Finset.sum_range_zero,
    Sendov911Box10_73.tab, Sendov911Box10_73.Adata, Sendov911Box10_73.Dscale,
    List.getD_cons_zero, List.getD_cons_succ, zero_add]
  norm_num
  ring

theorem red_73 (mu : ℂ) (Df : Fin 9 → ℂ) (rr : Fin 9 → ℝ) (a eta sig : ℝ)
    (h0 : (73/100 : ℝ) ≤ a) (h1 : a ≤ (37/50 : ℝ))
    (heta0 : 0 ≤ eta) (hnormSq : Complex.normSq mu = 1 - eta ^ 2)
    (hloc : (9:ℝ) ≤ 9 * a * mu.re + (1 - a ^ 2) * sig)
    (hsigeq : sig = ∑ k, 1 / rr k ^ 2)
    (hrlo : ∀ k, (309/500 : ℝ) ≤ rr k) (hrhi : ∀ k, rr k ≤ a + 1)
    (hprod : (10:ℝ) ≤ ∏ k, rr k)
    (hz : ∑ j, Df j = 0)
    (hD2 : ∑ j, ‖Df j‖ ^ 2 ≤ 9 * eta ^ 2)
    (hI : ‖∫ t in (0:ℝ)..a, ∏ j, ((1:ℂ) - (t:ℂ) * (mu + Df j))‖ < a / 10) :
    ∃ U V : ℝ, 0 ≤ U ∧ U ≤ 1 ∧ 0 ≤ V ∧ V ≤ 1 ∧
      Sendov911Capstone.Box.poly box_73 U V ≤ 0 := by
  have ha0 : (0:ℝ) < a := by linarith
  have ha1 : a ≤ 1 := by linarith
  have hsig : sig ≤ (1022103354851266015320143862892490866172561/122527345275878906250000000000000000000000 : ℝ) := by
    rw [hsigeq]
    exact Rows10.sigma_row_73 rr hrlo (fun k => le_trans (hrhi k) (by linarith)) hprod
  have hL := Rows10.L_row_73 h0 h1
  have hloc' : ((9:ℕ):ℝ) ≤ ((9:ℕ):ℝ) * a * mu.re + (1 - a ^ 2) * sig := by
    push_cast
    exact hloc
  have hkey := MidChainN.key_of_loc ha1 ha0.le hloc' hsig
  have hL' : 0 ≤ (1022103354851266015320143862892490866172561/122527345275878906250000000000000000000000 : ℝ) * a ^ 2 - ((9:ℕ):ℝ) * (77679/100000 : ℝ) * a + ((9:ℕ):ℝ) - (1022103354851266015320143862892490866172561/122527345275878906250000000000000000000000 : ℝ) := by
    push_cast
    exact hL
  have hlamu := MidChainN.lam_le_u (by norm_num) ha0 hkey hL'
  have hmunorm := MidChainN.lam_le_norm mu hlamu
  have hmu0 : mu ≠ 0 := by
    intro hcon
    rw [hcon] at hmunorm
    simp only [norm_zero] at hmunorm
    norm_num at hmunorm
  have hetale := MidChainN.eta_sq_le mu (by norm_num) hnormSq hmunorm
  have hetaY : eta ≤ (1968/3125 : ℝ) :=
    MidChainN.eta_le_Y (by norm_num) heta0 hetale Rows10.Y_row_73
  have hP := MidChainN.row_nonpos (N := 9) (alpha := (73/100 : ℝ)) (beta := (37/50 : ℝ))
    (S := (1022103354851266015320143862892490866172561/122527345275878906250000000000000000000000 : ℝ)) (lam := (77679/100000 : ℝ)) (sigma := sig) (by norm_num)
    CTab10.c10 (fun m _ => CTab10.c10_nonneg m) mu Df a eta
    (by norm_num) (by norm_num) (by norm_num) Rows10.lam_row_73 h0 h1
    heta0 hnormSq hloc' hsig hL' hz
    (CTab10.cTable10 Df eta heta0 hz hD2)
    (by push_cast; norm_num; exact JBoundN.J_closed_form_deg10 hmu0 a)
    (by push_cast; norm_num; exact hI)
  refine ⟨100 * (a - (73/100 : ℝ)), eta / (1968/3125 : ℝ), by linarith, by linarith,
    div_nonneg heta0 (by norm_num), (div_le_one (by norm_num)).mpr hetaY, ?_⟩
  have heq := eq_73 (100 * (a - (73/100 : ℝ))) (eta / (1968/3125 : ℝ))
  rw [show (73/100 : ℝ) + (1/100) * (100 * (a - (73/100 : ℝ))) = a by ring] at heq
  rw [show (1968/3125 : ℝ) * (eta / (1968/3125 : ℝ)) = eta by field_simp <;> ring] at heq
  exact le_of_eq_of_le heq.symm hP

/-! ### Row 74: `a ∈ [37/50, 3/4]`, S = 4120523269636747409577177209/490583516774400000000000000, λ = 488/625, Y = 62479/100000 -/

noncomputable def box_74 : Box where
  nx := 10
  ny := 10
  A := Sendov911Box10_74.tab Sendov911Box10_74.Adata
  D := (Sendov911Box10_74.Dscale : ℝ)
  hD := by norm_num [Sendov911Box10_74.Dscale]

theorem eq_74 (U V : ℝ) :
    (1 - ((37/50 : ℝ) + (1/100) * U)) / (((9:ℕ):ℝ) + 1) + ((62479/100000 : ℝ) * V) ^ 2 / (2 * (((9:ℕ):ℝ) + 1))
      - MidChainN.hh 9 (4120523269636747409577177209/490583516774400000000000000 : ℝ) ((37/50 : ℝ) + (1/100) * U) ((62479/100000 : ℝ) * V) ^ 5 / ((((9:ℕ):ℝ) + 1) * (488/625 : ℝ))
      - MidChainN.Emaj 9 CTab10.c10 (4120523269636747409577177209/490583516774400000000000000 : ℝ) ((37/50 : ℝ) + (1/100) * U) ((62479/100000 : ℝ) * V)
    = ∑ k ∈ Finset.range 11, ∑ l ∈ Finset.range 11,
        ((Sendov911Box10_74.tab Sendov911Box10_74.Adata k l : ℝ)
          / (Sendov911Box10_74.Dscale : ℝ)) * U ^ k * V ^ l := by
  rw [EmajEq.Emaj10_eq]
  unfold MidChainN.hh
  push_cast
  simp only [Finset.sum_range_succ, Finset.sum_range_zero,
    Sendov911Box10_74.tab, Sendov911Box10_74.Adata, Sendov911Box10_74.Dscale,
    List.getD_cons_zero, List.getD_cons_succ, zero_add]
  norm_num
  ring

theorem red_74 (mu : ℂ) (Df : Fin 9 → ℂ) (rr : Fin 9 → ℝ) (a eta sig : ℝ)
    (h0 : (37/50 : ℝ) ≤ a) (h1 : a ≤ (3/4 : ℝ))
    (heta0 : 0 ≤ eta) (hnormSq : Complex.normSq mu = 1 - eta ^ 2)
    (hloc : (9:ℝ) ≤ 9 * a * mu.re + (1 - a ^ 2) * sig)
    (hsigeq : sig = ∑ k, 1 / rr k ^ 2)
    (hrlo : ∀ k, (309/500 : ℝ) ≤ rr k) (hrhi : ∀ k, rr k ≤ a + 1)
    (hprod : (10:ℝ) ≤ ∏ k, rr k)
    (hz : ∑ j, Df j = 0)
    (hD2 : ∑ j, ‖Df j‖ ^ 2 ≤ 9 * eta ^ 2)
    (hI : ‖∫ t in (0:ℝ)..a, ∏ j, ((1:ℂ) - (t:ℂ) * (mu + Df j))‖ < a / 10) :
    ∃ U V : ℝ, 0 ≤ U ∧ U ≤ 1 ∧ 0 ≤ V ∧ V ≤ 1 ∧
      Sendov911Capstone.Box.poly box_74 U V ≤ 0 := by
  have ha0 : (0:ℝ) < a := by linarith
  have ha1 : a ≤ 1 := by linarith
  have hsig : sig ≤ (4120523269636747409577177209/490583516774400000000000000 : ℝ) := by
    rw [hsigeq]
    exact Rows10.sigma_row_74 rr hrlo (fun k => le_trans (hrhi k) (by linarith)) hprod
  have hL := Rows10.L_row_74 h0 h1
  have hloc' : ((9:ℕ):ℝ) ≤ ((9:ℕ):ℝ) * a * mu.re + (1 - a ^ 2) * sig := by
    push_cast
    exact hloc
  have hkey := MidChainN.key_of_loc ha1 ha0.le hloc' hsig
  have hL' : 0 ≤ (4120523269636747409577177209/490583516774400000000000000 : ℝ) * a ^ 2 - ((9:ℕ):ℝ) * (488/625 : ℝ) * a + ((9:ℕ):ℝ) - (4120523269636747409577177209/490583516774400000000000000 : ℝ) := by
    push_cast
    exact hL
  have hlamu := MidChainN.lam_le_u (by norm_num) ha0 hkey hL'
  have hmunorm := MidChainN.lam_le_norm mu hlamu
  have hmu0 : mu ≠ 0 := by
    intro hcon
    rw [hcon] at hmunorm
    simp only [norm_zero] at hmunorm
    norm_num at hmunorm
  have hetale := MidChainN.eta_sq_le mu (by norm_num) hnormSq hmunorm
  have hetaY : eta ≤ (62479/100000 : ℝ) :=
    MidChainN.eta_le_Y (by norm_num) heta0 hetale Rows10.Y_row_74
  have hP := MidChainN.row_nonpos (N := 9) (alpha := (37/50 : ℝ)) (beta := (3/4 : ℝ))
    (S := (4120523269636747409577177209/490583516774400000000000000 : ℝ)) (lam := (488/625 : ℝ)) (sigma := sig) (by norm_num)
    CTab10.c10 (fun m _ => CTab10.c10_nonneg m) mu Df a eta
    (by norm_num) (by norm_num) (by norm_num) Rows10.lam_row_74 h0 h1
    heta0 hnormSq hloc' hsig hL' hz
    (CTab10.cTable10 Df eta heta0 hz hD2)
    (by push_cast; norm_num; exact JBoundN.J_closed_form_deg10 hmu0 a)
    (by push_cast; norm_num; exact hI)
  refine ⟨100 * (a - (37/50 : ℝ)), eta / (62479/100000 : ℝ), by linarith, by linarith,
    div_nonneg heta0 (by norm_num), (div_le_one (by norm_num)).mpr hetaY, ?_⟩
  have heq := eq_74 (100 * (a - (37/50 : ℝ))) (eta / (62479/100000 : ℝ))
  rw [show (37/50 : ℝ) + (1/100) * (100 * (a - (37/50 : ℝ))) = a by ring] at heq
  rw [show (62479/100000 : ℝ) * (eta / (62479/100000 : ℝ)) = eta by field_simp <;> ring] at heq
  exact le_of_eq_of_le heq.symm hP

/-! ### Row 75: `a ∈ [3/4, 19/25]`, S = 284534749622339733831287804932159507/33624240313656628131866455078125000, λ = 15697/20000, Y = 61969/100000 -/

noncomputable def box_75 : Box where
  nx := 10
  ny := 10
  A := Sendov911Box10_75.tab Sendov911Box10_75.Adata
  D := (Sendov911Box10_75.Dscale : ℝ)
  hD := by norm_num [Sendov911Box10_75.Dscale]

theorem eq_75 (U V : ℝ) :
    (1 - ((3/4 : ℝ) + (1/100) * U)) / (((9:ℕ):ℝ) + 1) + ((61969/100000 : ℝ) * V) ^ 2 / (2 * (((9:ℕ):ℝ) + 1))
      - MidChainN.hh 9 (284534749622339733831287804932159507/33624240313656628131866455078125000 : ℝ) ((3/4 : ℝ) + (1/100) * U) ((61969/100000 : ℝ) * V) ^ 5 / ((((9:ℕ):ℝ) + 1) * (15697/20000 : ℝ))
      - MidChainN.Emaj 9 CTab10.c10 (284534749622339733831287804932159507/33624240313656628131866455078125000 : ℝ) ((3/4 : ℝ) + (1/100) * U) ((61969/100000 : ℝ) * V)
    = ∑ k ∈ Finset.range 11, ∑ l ∈ Finset.range 11,
        ((Sendov911Box10_75.tab Sendov911Box10_75.Adata k l : ℝ)
          / (Sendov911Box10_75.Dscale : ℝ)) * U ^ k * V ^ l := by
  rw [EmajEq.Emaj10_eq]
  unfold MidChainN.hh
  push_cast
  simp only [Finset.sum_range_succ, Finset.sum_range_zero,
    Sendov911Box10_75.tab, Sendov911Box10_75.Adata, Sendov911Box10_75.Dscale,
    List.getD_cons_zero, List.getD_cons_succ, zero_add]
  norm_num
  ring

theorem red_75 (mu : ℂ) (Df : Fin 9 → ℂ) (rr : Fin 9 → ℝ) (a eta sig : ℝ)
    (h0 : (3/4 : ℝ) ≤ a) (h1 : a ≤ (19/25 : ℝ))
    (heta0 : 0 ≤ eta) (hnormSq : Complex.normSq mu = 1 - eta ^ 2)
    (hloc : (9:ℝ) ≤ 9 * a * mu.re + (1 - a ^ 2) * sig)
    (hsigeq : sig = ∑ k, 1 / rr k ^ 2)
    (hrlo : ∀ k, (309/500 : ℝ) ≤ rr k) (hrhi : ∀ k, rr k ≤ a + 1)
    (hprod : (10:ℝ) ≤ ∏ k, rr k)
    (hz : ∑ j, Df j = 0)
    (hD2 : ∑ j, ‖Df j‖ ^ 2 ≤ 9 * eta ^ 2)
    (hI : ‖∫ t in (0:ℝ)..a, ∏ j, ((1:ℂ) - (t:ℂ) * (mu + Df j))‖ < a / 10) :
    ∃ U V : ℝ, 0 ≤ U ∧ U ≤ 1 ∧ 0 ≤ V ∧ V ≤ 1 ∧
      Sendov911Capstone.Box.poly box_75 U V ≤ 0 := by
  have ha0 : (0:ℝ) < a := by linarith
  have ha1 : a ≤ 1 := by linarith
  have hsig : sig ≤ (284534749622339733831287804932159507/33624240313656628131866455078125000 : ℝ) := by
    rw [hsigeq]
    exact Rows10.sigma_row_75 rr hrlo (fun k => le_trans (hrhi k) (by linarith)) hprod
  have hL := Rows10.L_row_75 h0 h1
  have hloc' : ((9:ℕ):ℝ) ≤ ((9:ℕ):ℝ) * a * mu.re + (1 - a ^ 2) * sig := by
    push_cast
    exact hloc
  have hkey := MidChainN.key_of_loc ha1 ha0.le hloc' hsig
  have hL' : 0 ≤ (284534749622339733831287804932159507/33624240313656628131866455078125000 : ℝ) * a ^ 2 - ((9:ℕ):ℝ) * (15697/20000 : ℝ) * a + ((9:ℕ):ℝ) - (284534749622339733831287804932159507/33624240313656628131866455078125000 : ℝ) := by
    push_cast
    exact hL
  have hlamu := MidChainN.lam_le_u (by norm_num) ha0 hkey hL'
  have hmunorm := MidChainN.lam_le_norm mu hlamu
  have hmu0 : mu ≠ 0 := by
    intro hcon
    rw [hcon] at hmunorm
    simp only [norm_zero] at hmunorm
    norm_num at hmunorm
  have hetale := MidChainN.eta_sq_le mu (by norm_num) hnormSq hmunorm
  have hetaY : eta ≤ (61969/100000 : ℝ) :=
    MidChainN.eta_le_Y (by norm_num) heta0 hetale Rows10.Y_row_75
  have hP := MidChainN.row_nonpos (N := 9) (alpha := (3/4 : ℝ)) (beta := (19/25 : ℝ))
    (S := (284534749622339733831287804932159507/33624240313656628131866455078125000 : ℝ)) (lam := (15697/20000 : ℝ)) (sigma := sig) (by norm_num)
    CTab10.c10 (fun m _ => CTab10.c10_nonneg m) mu Df a eta
    (by norm_num) (by norm_num) (by norm_num) Rows10.lam_row_75 h0 h1
    heta0 hnormSq hloc' hsig hL' hz
    (CTab10.cTable10 Df eta heta0 hz hD2)
    (by push_cast; norm_num; exact JBoundN.J_closed_form_deg10 hmu0 a)
    (by push_cast; norm_num; exact hI)
  refine ⟨100 * (a - (3/4 : ℝ)), eta / (61969/100000 : ℝ), by linarith, by linarith,
    div_nonneg heta0 (by norm_num), (div_le_one (by norm_num)).mpr hetaY, ?_⟩
  have heq := eq_75 (100 * (a - (3/4 : ℝ))) (eta / (61969/100000 : ℝ))
  rw [show (3/4 : ℝ) + (1/100) * (100 * (a - (3/4 : ℝ))) = a by ring] at heq
  rw [show (61969/100000 : ℝ) * (eta / (61969/100000 : ℝ)) = eta by field_simp <;> ring] at heq
  exact le_of_eq_of_le heq.symm hP

/-! ### Row 76: `a ∈ [19/25, 77/100]`, S = 17721562472536429401337990666569828960802642241/2077308506250000000000000000000000000000000000, λ = 4931/6250, Y = 12289/20000 -/

noncomputable def box_76 : Box where
  nx := 10
  ny := 10
  A := Sendov911Box10_76.tab Sendov911Box10_76.Adata
  D := (Sendov911Box10_76.Dscale : ℝ)
  hD := by norm_num [Sendov911Box10_76.Dscale]

theorem eq_76 (U V : ℝ) :
    (1 - ((19/25 : ℝ) + (1/100) * U)) / (((9:ℕ):ℝ) + 1) + ((12289/20000 : ℝ) * V) ^ 2 / (2 * (((9:ℕ):ℝ) + 1))
      - MidChainN.hh 9 (17721562472536429401337990666569828960802642241/2077308506250000000000000000000000000000000000 : ℝ) ((19/25 : ℝ) + (1/100) * U) ((12289/20000 : ℝ) * V) ^ 5 / ((((9:ℕ):ℝ) + 1) * (4931/6250 : ℝ))
      - MidChainN.Emaj 9 CTab10.c10 (17721562472536429401337990666569828960802642241/2077308506250000000000000000000000000000000000 : ℝ) ((19/25 : ℝ) + (1/100) * U) ((12289/20000 : ℝ) * V)
    = ∑ k ∈ Finset.range 11, ∑ l ∈ Finset.range 11,
        ((Sendov911Box10_76.tab Sendov911Box10_76.Adata k l : ℝ)
          / (Sendov911Box10_76.Dscale : ℝ)) * U ^ k * V ^ l := by
  rw [EmajEq.Emaj10_eq]
  unfold MidChainN.hh
  push_cast
  simp only [Finset.sum_range_succ, Finset.sum_range_zero,
    Sendov911Box10_76.tab, Sendov911Box10_76.Adata, Sendov911Box10_76.Dscale,
    List.getD_cons_zero, List.getD_cons_succ, zero_add]
  norm_num
  ring

theorem red_76 (mu : ℂ) (Df : Fin 9 → ℂ) (rr : Fin 9 → ℝ) (a eta sig : ℝ)
    (h0 : (19/25 : ℝ) ≤ a) (h1 : a ≤ (77/100 : ℝ))
    (heta0 : 0 ≤ eta) (hnormSq : Complex.normSq mu = 1 - eta ^ 2)
    (hloc : (9:ℝ) ≤ 9 * a * mu.re + (1 - a ^ 2) * sig)
    (hsigeq : sig = ∑ k, 1 / rr k ^ 2)
    (hrlo : ∀ k, (309/500 : ℝ) ≤ rr k) (hrhi : ∀ k, rr k ≤ a + 1)
    (hprod : (10:ℝ) ≤ ∏ k, rr k)
    (hz : ∑ j, Df j = 0)
    (hD2 : ∑ j, ‖Df j‖ ^ 2 ≤ 9 * eta ^ 2)
    (hI : ‖∫ t in (0:ℝ)..a, ∏ j, ((1:ℂ) - (t:ℂ) * (mu + Df j))‖ < a / 10) :
    ∃ U V : ℝ, 0 ≤ U ∧ U ≤ 1 ∧ 0 ≤ V ∧ V ≤ 1 ∧
      Sendov911Capstone.Box.poly box_76 U V ≤ 0 := by
  have ha0 : (0:ℝ) < a := by linarith
  have ha1 : a ≤ 1 := by linarith
  have hsig : sig ≤ (17721562472536429401337990666569828960802642241/2077308506250000000000000000000000000000000000 : ℝ) := by
    rw [hsigeq]
    exact Rows10.sigma_row_76 rr hrlo (fun k => le_trans (hrhi k) (by linarith)) hprod
  have hL := Rows10.L_row_76 h0 h1
  have hloc' : ((9:ℕ):ℝ) ≤ ((9:ℕ):ℝ) * a * mu.re + (1 - a ^ 2) * sig := by
    push_cast
    exact hloc
  have hkey := MidChainN.key_of_loc ha1 ha0.le hloc' hsig
  have hL' : 0 ≤ (17721562472536429401337990666569828960802642241/2077308506250000000000000000000000000000000000 : ℝ) * a ^ 2 - ((9:ℕ):ℝ) * (4931/6250 : ℝ) * a + ((9:ℕ):ℝ) - (17721562472536429401337990666569828960802642241/2077308506250000000000000000000000000000000000 : ℝ) := by
    push_cast
    exact hL
  have hlamu := MidChainN.lam_le_u (by norm_num) ha0 hkey hL'
  have hmunorm := MidChainN.lam_le_norm mu hlamu
  have hmu0 : mu ≠ 0 := by
    intro hcon
    rw [hcon] at hmunorm
    simp only [norm_zero] at hmunorm
    norm_num at hmunorm
  have hetale := MidChainN.eta_sq_le mu (by norm_num) hnormSq hmunorm
  have hetaY : eta ≤ (12289/20000 : ℝ) :=
    MidChainN.eta_le_Y (by norm_num) heta0 hetale Rows10.Y_row_76
  have hP := MidChainN.row_nonpos (N := 9) (alpha := (19/25 : ℝ)) (beta := (77/100 : ℝ))
    (S := (17721562472536429401337990666569828960802642241/2077308506250000000000000000000000000000000000 : ℝ)) (lam := (4931/6250 : ℝ)) (sigma := sig) (by norm_num)
    CTab10.c10 (fun m _ => CTab10.c10_nonneg m) mu Df a eta
    (by norm_num) (by norm_num) (by norm_num) Rows10.lam_row_76 h0 h1
    heta0 hnormSq hloc' hsig hL' hz
    (CTab10.cTable10 Df eta heta0 hz hD2)
    (by push_cast; norm_num; exact JBoundN.J_closed_form_deg10 hmu0 a)
    (by push_cast; norm_num; exact hI)
  refine ⟨100 * (a - (19/25 : ℝ)), eta / (12289/20000 : ℝ), by linarith, by linarith,
    div_nonneg heta0 (by norm_num), (div_le_one (by norm_num)).mpr hetaY, ?_⟩
  have heq := eq_76 (100 * (a - (19/25 : ℝ))) (eta / (12289/20000 : ℝ))
  rw [show (19/25 : ℝ) + (1/100) * (100 * (a - (19/25 : ℝ))) = a by ring] at heq
  rw [show (12289/20000 : ℝ) * (eta / (12289/20000 : ℝ)) = eta by field_simp <;> ring] at heq
  exact le_of_eq_of_le heq.symm hP

/-! ### Row 77: `a ∈ [77/100, 39/50]`, S = 9931615641919803804024643242236885992817081/1154029847717285156250000000000000000000000, λ = 39657/50000, Y = 7613/12500 -/

noncomputable def box_77 : Box where
  nx := 10
  ny := 10
  A := Sendov911Box10_77.tab Sendov911Box10_77.Adata
  D := (Sendov911Box10_77.Dscale : ℝ)
  hD := by norm_num [Sendov911Box10_77.Dscale]

theorem eq_77 (U V : ℝ) :
    (1 - ((77/100 : ℝ) + (1/100) * U)) / (((9:ℕ):ℝ) + 1) + ((7613/12500 : ℝ) * V) ^ 2 / (2 * (((9:ℕ):ℝ) + 1))
      - MidChainN.hh 9 (9931615641919803804024643242236885992817081/1154029847717285156250000000000000000000000 : ℝ) ((77/100 : ℝ) + (1/100) * U) ((7613/12500 : ℝ) * V) ^ 5 / ((((9:ℕ):ℝ) + 1) * (39657/50000 : ℝ))
      - MidChainN.Emaj 9 CTab10.c10 (9931615641919803804024643242236885992817081/1154029847717285156250000000000000000000000 : ℝ) ((77/100 : ℝ) + (1/100) * U) ((7613/12500 : ℝ) * V)
    = ∑ k ∈ Finset.range 11, ∑ l ∈ Finset.range 11,
        ((Sendov911Box10_77.tab Sendov911Box10_77.Adata k l : ℝ)
          / (Sendov911Box10_77.Dscale : ℝ)) * U ^ k * V ^ l := by
  rw [EmajEq.Emaj10_eq]
  unfold MidChainN.hh
  push_cast
  simp only [Finset.sum_range_succ, Finset.sum_range_zero,
    Sendov911Box10_77.tab, Sendov911Box10_77.Adata, Sendov911Box10_77.Dscale,
    List.getD_cons_zero, List.getD_cons_succ, zero_add]
  norm_num
  ring

theorem red_77 (mu : ℂ) (Df : Fin 9 → ℂ) (rr : Fin 9 → ℝ) (a eta sig : ℝ)
    (h0 : (77/100 : ℝ) ≤ a) (h1 : a ≤ (39/50 : ℝ))
    (heta0 : 0 ≤ eta) (hnormSq : Complex.normSq mu = 1 - eta ^ 2)
    (hloc : (9:ℝ) ≤ 9 * a * mu.re + (1 - a ^ 2) * sig)
    (hsigeq : sig = ∑ k, 1 / rr k ^ 2)
    (hrlo : ∀ k, (309/500 : ℝ) ≤ rr k) (hrhi : ∀ k, rr k ≤ a + 1)
    (hprod : (10:ℝ) ≤ ∏ k, rr k)
    (hz : ∑ j, Df j = 0)
    (hD2 : ∑ j, ‖Df j‖ ^ 2 ≤ 9 * eta ^ 2)
    (hI : ‖∫ t in (0:ℝ)..a, ∏ j, ((1:ℂ) - (t:ℂ) * (mu + Df j))‖ < a / 10) :
    ∃ U V : ℝ, 0 ≤ U ∧ U ≤ 1 ∧ 0 ≤ V ∧ V ≤ 1 ∧
      Sendov911Capstone.Box.poly box_77 U V ≤ 0 := by
  have ha0 : (0:ℝ) < a := by linarith
  have ha1 : a ≤ 1 := by linarith
  have hsig : sig ≤ (9931615641919803804024643242236885992817081/1154029847717285156250000000000000000000000 : ℝ) := by
    rw [hsigeq]
    exact Rows10.sigma_row_77 rr hrlo (fun k => le_trans (hrhi k) (by linarith)) hprod
  have hL := Rows10.L_row_77 h0 h1
  have hloc' : ((9:ℕ):ℝ) ≤ ((9:ℕ):ℝ) * a * mu.re + (1 - a ^ 2) * sig := by
    push_cast
    exact hloc
  have hkey := MidChainN.key_of_loc ha1 ha0.le hloc' hsig
  have hL' : 0 ≤ (9931615641919803804024643242236885992817081/1154029847717285156250000000000000000000000 : ℝ) * a ^ 2 - ((9:ℕ):ℝ) * (39657/50000 : ℝ) * a + ((9:ℕ):ℝ) - (9931615641919803804024643242236885992817081/1154029847717285156250000000000000000000000 : ℝ) := by
    push_cast
    exact hL
  have hlamu := MidChainN.lam_le_u (by norm_num) ha0 hkey hL'
  have hmunorm := MidChainN.lam_le_norm mu hlamu
  have hmu0 : mu ≠ 0 := by
    intro hcon
    rw [hcon] at hmunorm
    simp only [norm_zero] at hmunorm
    norm_num at hmunorm
  have hetale := MidChainN.eta_sq_le mu (by norm_num) hnormSq hmunorm
  have hetaY : eta ≤ (7613/12500 : ℝ) :=
    MidChainN.eta_le_Y (by norm_num) heta0 hetale Rows10.Y_row_77
  have hP := MidChainN.row_nonpos (N := 9) (alpha := (77/100 : ℝ)) (beta := (39/50 : ℝ))
    (S := (9931615641919803804024643242236885992817081/1154029847717285156250000000000000000000000 : ℝ)) (lam := (39657/50000 : ℝ)) (sigma := sig) (by norm_num)
    CTab10.c10 (fun m _ => CTab10.c10_nonneg m) mu Df a eta
    (by norm_num) (by norm_num) (by norm_num) Rows10.lam_row_77 h0 h1
    heta0 hnormSq hloc' hsig hL' hz
    (CTab10.cTable10 Df eta heta0 hz hD2)
    (by push_cast; norm_num; exact JBoundN.J_closed_form_deg10 hmu0 a)
    (by push_cast; norm_num; exact hI)
  refine ⟨100 * (a - (77/100 : ℝ)), eta / (7613/12500 : ℝ), by linarith, by linarith,
    div_nonneg heta0 (by norm_num), (div_le_one (by norm_num)).mpr hetaY, ?_⟩
  have heq := eq_77 (100 * (a - (77/100 : ℝ))) (eta / (7613/12500 : ℝ))
  rw [show (77/100 : ℝ) + (1/100) * (100 * (a - (77/100 : ℝ))) = a by ring] at heq
  rw [show (7613/12500 : ℝ) * (eta / (7613/12500 : ℝ)) = eta by field_simp <;> ring] at heq
  exact le_of_eq_of_le heq.symm hP

/-! ### Row 78: `a ∈ [39/50, 79/100]`, S = 166111698461245760247164405576129079822519190721/19120667006250000000000000000000000000000000000, λ = 39871/50000, Y = 60343/100000 -/

noncomputable def box_78 : Box where
  nx := 10
  ny := 10
  A := Sendov911Box10_78.tab Sendov911Box10_78.Adata
  D := (Sendov911Box10_78.Dscale : ℝ)
  hD := by norm_num [Sendov911Box10_78.Dscale]

theorem eq_78 (U V : ℝ) :
    (1 - ((39/50 : ℝ) + (1/100) * U)) / (((9:ℕ):ℝ) + 1) + ((60343/100000 : ℝ) * V) ^ 2 / (2 * (((9:ℕ):ℝ) + 1))
      - MidChainN.hh 9 (166111698461245760247164405576129079822519190721/19120667006250000000000000000000000000000000000 : ℝ) ((39/50 : ℝ) + (1/100) * U) ((60343/100000 : ℝ) * V) ^ 5 / ((((9:ℕ):ℝ) + 1) * (39871/50000 : ℝ))
      - MidChainN.Emaj 9 CTab10.c10 (166111698461245760247164405576129079822519190721/19120667006250000000000000000000000000000000000 : ℝ) ((39/50 : ℝ) + (1/100) * U) ((60343/100000 : ℝ) * V)
    = ∑ k ∈ Finset.range 11, ∑ l ∈ Finset.range 11,
        ((Sendov911Box10_78.tab Sendov911Box10_78.Adata k l : ℝ)
          / (Sendov911Box10_78.Dscale : ℝ)) * U ^ k * V ^ l := by
  rw [EmajEq.Emaj10_eq]
  unfold MidChainN.hh
  push_cast
  simp only [Finset.sum_range_succ, Finset.sum_range_zero,
    Sendov911Box10_78.tab, Sendov911Box10_78.Adata, Sendov911Box10_78.Dscale,
    List.getD_cons_zero, List.getD_cons_succ, zero_add]
  norm_num
  ring

theorem red_78 (mu : ℂ) (Df : Fin 9 → ℂ) (rr : Fin 9 → ℝ) (a eta sig : ℝ)
    (h0 : (39/50 : ℝ) ≤ a) (h1 : a ≤ (79/100 : ℝ))
    (heta0 : 0 ≤ eta) (hnormSq : Complex.normSq mu = 1 - eta ^ 2)
    (hloc : (9:ℝ) ≤ 9 * a * mu.re + (1 - a ^ 2) * sig)
    (hsigeq : sig = ∑ k, 1 / rr k ^ 2)
    (hrlo : ∀ k, (309/500 : ℝ) ≤ rr k) (hrhi : ∀ k, rr k ≤ a + 1)
    (hprod : (10:ℝ) ≤ ∏ k, rr k)
    (hz : ∑ j, Df j = 0)
    (hD2 : ∑ j, ‖Df j‖ ^ 2 ≤ 9 * eta ^ 2)
    (hI : ‖∫ t in (0:ℝ)..a, ∏ j, ((1:ℂ) - (t:ℂ) * (mu + Df j))‖ < a / 10) :
    ∃ U V : ℝ, 0 ≤ U ∧ U ≤ 1 ∧ 0 ≤ V ∧ V ≤ 1 ∧
      Sendov911Capstone.Box.poly box_78 U V ≤ 0 := by
  have ha0 : (0:ℝ) < a := by linarith
  have ha1 : a ≤ 1 := by linarith
  have hsig : sig ≤ (166111698461245760247164405576129079822519190721/19120667006250000000000000000000000000000000000 : ℝ) := by
    rw [hsigeq]
    exact Rows10.sigma_row_78 rr hrlo (fun k => le_trans (hrhi k) (by linarith)) hprod
  have hL := Rows10.L_row_78 h0 h1
  have hloc' : ((9:ℕ):ℝ) ≤ ((9:ℕ):ℝ) * a * mu.re + (1 - a ^ 2) * sig := by
    push_cast
    exact hloc
  have hkey := MidChainN.key_of_loc ha1 ha0.le hloc' hsig
  have hL' : 0 ≤ (166111698461245760247164405576129079822519190721/19120667006250000000000000000000000000000000000 : ℝ) * a ^ 2 - ((9:ℕ):ℝ) * (39871/50000 : ℝ) * a + ((9:ℕ):ℝ) - (166111698461245760247164405576129079822519190721/19120667006250000000000000000000000000000000000 : ℝ) := by
    push_cast
    exact hL
  have hlamu := MidChainN.lam_le_u (by norm_num) ha0 hkey hL'
  have hmunorm := MidChainN.lam_le_norm mu hlamu
  have hmu0 : mu ≠ 0 := by
    intro hcon
    rw [hcon] at hmunorm
    simp only [norm_zero] at hmunorm
    norm_num at hmunorm
  have hetale := MidChainN.eta_sq_le mu (by norm_num) hnormSq hmunorm
  have hetaY : eta ≤ (60343/100000 : ℝ) :=
    MidChainN.eta_le_Y (by norm_num) heta0 hetale Rows10.Y_row_78
  have hP := MidChainN.row_nonpos (N := 9) (alpha := (39/50 : ℝ)) (beta := (79/100 : ℝ))
    (S := (166111698461245760247164405576129079822519190721/19120667006250000000000000000000000000000000000 : ℝ)) (lam := (39871/50000 : ℝ)) (sigma := sig) (by norm_num)
    CTab10.c10 (fun m _ => CTab10.c10_nonneg m) mu Df a eta
    (by norm_num) (by norm_num) (by norm_num) Rows10.lam_row_78 h0 h1
    heta0 hnormSq hloc' hsig hL' hz
    (CTab10.cTable10 Df eta heta0 hz hD2)
    (by push_cast; norm_num; exact JBoundN.J_closed_form_deg10 hmu0 a)
    (by push_cast; norm_num; exact hI)
  refine ⟨100 * (a - (39/50 : ℝ)), eta / (60343/100000 : ℝ), by linarith, by linarith,
    div_nonneg heta0 (by norm_num), (div_le_one (by norm_num)).mpr hetaY, ?_⟩
  have heq := eq_78 (100 * (a - (39/50 : ℝ))) (eta / (60343/100000 : ℝ))
  rw [show (39/50 : ℝ) + (1/100) * (100 * (a - (39/50 : ℝ))) = a by ring] at heq
  rw [show (60343/100000 : ℝ) * (eta / (60343/100000 : ℝ)) = eta by field_simp <;> ring] at heq
  exact le_of_eq_of_le heq.symm hP

/-! ### Row 79: `a ∈ [79/100, 4/5]`, S = 3835755162533026509926519963/437077331542968750000000000, λ = 10023/12500, Y = 29877/50000 -/

noncomputable def box_79 : Box where
  nx := 10
  ny := 10
  A := Sendov911Box10_79.tab Sendov911Box10_79.Adata
  D := (Sendov911Box10_79.Dscale : ℝ)
  hD := by norm_num [Sendov911Box10_79.Dscale]

theorem eq_79 (U V : ℝ) :
    (1 - ((79/100 : ℝ) + (1/100) * U)) / (((9:ℕ):ℝ) + 1) + ((29877/50000 : ℝ) * V) ^ 2 / (2 * (((9:ℕ):ℝ) + 1))
      - MidChainN.hh 9 (3835755162533026509926519963/437077331542968750000000000 : ℝ) ((79/100 : ℝ) + (1/100) * U) ((29877/50000 : ℝ) * V) ^ 5 / ((((9:ℕ):ℝ) + 1) * (10023/12500 : ℝ))
      - MidChainN.Emaj 9 CTab10.c10 (3835755162533026509926519963/437077331542968750000000000 : ℝ) ((79/100 : ℝ) + (1/100) * U) ((29877/50000 : ℝ) * V)
    = ∑ k ∈ Finset.range 11, ∑ l ∈ Finset.range 11,
        ((Sendov911Box10_79.tab Sendov911Box10_79.Adata k l : ℝ)
          / (Sendov911Box10_79.Dscale : ℝ)) * U ^ k * V ^ l := by
  rw [EmajEq.Emaj10_eq]
  unfold MidChainN.hh
  push_cast
  simp only [Finset.sum_range_succ, Finset.sum_range_zero,
    Sendov911Box10_79.tab, Sendov911Box10_79.Adata, Sendov911Box10_79.Dscale,
    List.getD_cons_zero, List.getD_cons_succ, zero_add]
  norm_num
  ring

theorem red_79 (mu : ℂ) (Df : Fin 9 → ℂ) (rr : Fin 9 → ℝ) (a eta sig : ℝ)
    (h0 : (79/100 : ℝ) ≤ a) (h1 : a ≤ (4/5 : ℝ))
    (heta0 : 0 ≤ eta) (hnormSq : Complex.normSq mu = 1 - eta ^ 2)
    (hloc : (9:ℝ) ≤ 9 * a * mu.re + (1 - a ^ 2) * sig)
    (hsigeq : sig = ∑ k, 1 / rr k ^ 2)
    (hrlo : ∀ k, (309/500 : ℝ) ≤ rr k) (hrhi : ∀ k, rr k ≤ a + 1)
    (hprod : (10:ℝ) ≤ ∏ k, rr k)
    (hz : ∑ j, Df j = 0)
    (hD2 : ∑ j, ‖Df j‖ ^ 2 ≤ 9 * eta ^ 2)
    (hI : ‖∫ t in (0:ℝ)..a, ∏ j, ((1:ℂ) - (t:ℂ) * (mu + Df j))‖ < a / 10) :
    ∃ U V : ℝ, 0 ≤ U ∧ U ≤ 1 ∧ 0 ≤ V ∧ V ≤ 1 ∧
      Sendov911Capstone.Box.poly box_79 U V ≤ 0 := by
  have ha0 : (0:ℝ) < a := by linarith
  have ha1 : a ≤ 1 := by linarith
  have hsig : sig ≤ (3835755162533026509926519963/437077331542968750000000000 : ℝ) := by
    rw [hsigeq]
    exact Rows10.sigma_row_79 rr hrlo (fun k => le_trans (hrhi k) (by linarith)) hprod
  have hL := Rows10.L_row_79 h0 h1
  have hloc' : ((9:ℕ):ℝ) ≤ ((9:ℕ):ℝ) * a * mu.re + (1 - a ^ 2) * sig := by
    push_cast
    exact hloc
  have hkey := MidChainN.key_of_loc ha1 ha0.le hloc' hsig
  have hL' : 0 ≤ (3835755162533026509926519963/437077331542968750000000000 : ℝ) * a ^ 2 - ((9:ℕ):ℝ) * (10023/12500 : ℝ) * a + ((9:ℕ):ℝ) - (3835755162533026509926519963/437077331542968750000000000 : ℝ) := by
    push_cast
    exact hL
  have hlamu := MidChainN.lam_le_u (by norm_num) ha0 hkey hL'
  have hmunorm := MidChainN.lam_le_norm mu hlamu
  have hmu0 : mu ≠ 0 := by
    intro hcon
    rw [hcon] at hmunorm
    simp only [norm_zero] at hmunorm
    norm_num at hmunorm
  have hetale := MidChainN.eta_sq_le mu (by norm_num) hnormSq hmunorm
  have hetaY : eta ≤ (29877/50000 : ℝ) :=
    MidChainN.eta_le_Y (by norm_num) heta0 hetale Rows10.Y_row_79
  have hP := MidChainN.row_nonpos (N := 9) (alpha := (79/100 : ℝ)) (beta := (4/5 : ℝ))
    (S := (3835755162533026509926519963/437077331542968750000000000 : ℝ)) (lam := (10023/12500 : ℝ)) (sigma := sig) (by norm_num)
    CTab10.c10 (fun m _ => CTab10.c10_nonneg m) mu Df a eta
    (by norm_num) (by norm_num) (by norm_num) Rows10.lam_row_79 h0 h1
    heta0 hnormSq hloc' hsig hL' hz
    (CTab10.cTable10 Df eta heta0 hz hD2)
    (by push_cast; norm_num; exact JBoundN.J_closed_form_deg10 hmu0 a)
    (by push_cast; norm_num; exact hI)
  refine ⟨100 * (a - (79/100 : ℝ)), eta / (29877/50000 : ℝ), by linarith, by linarith,
    div_nonneg heta0 (by norm_num), (div_le_one (by norm_num)).mpr hetaY, ?_⟩
  have heq := eq_79 (100 * (a - (79/100 : ℝ))) (eta / (29877/50000 : ℝ))
  rw [show (79/100 : ℝ) + (1/100) * (100 * (a - (79/100 : ℝ))) = a by ring] at heq
  rw [show (29877/50000 : ℝ) * (eta / (29877/50000 : ℝ)) = eta by field_simp <;> ring] at heq
  exact le_of_eq_of_le heq.symm hP

noncomputable def chunk : List Box :=
  [box_70, box_71, box_72, box_73, box_74, box_75, box_76, box_77, box_78, box_79]

theorem chunkPos : CoveringPositive chunk := by
  unfold chunk
  refine coveringPositive_cons (fun _ _ h1 h2 h3 h4 => Sendov911Box10_70.box_positive h1 h2 h3 h4)
    (coveringPositive_cons (fun _ _ h1 h2 h3 h4 => Sendov911Box10_71.box_positive h1 h2 h3 h4)
    (coveringPositive_cons (fun _ _ h1 h2 h3 h4 => Sendov911Box10_72.box_positive h1 h2 h3 h4)
    (coveringPositive_cons (fun _ _ h1 h2 h3 h4 => Sendov911Box10_73.box_positive h1 h2 h3 h4)
    (coveringPositive_cons (fun _ _ h1 h2 h3 h4 => Sendov911Box10_74.box_positive h1 h2 h3 h4)
    (coveringPositive_cons (fun _ _ h1 h2 h3 h4 => Sendov911Box10_75.box_positive h1 h2 h3 h4)
    (coveringPositive_cons (fun _ _ h1 h2 h3 h4 => Sendov911Box10_76.box_positive h1 h2 h3 h4)
    (coveringPositive_cons (fun _ _ h1 h2 h3 h4 => Sendov911Box10_77.box_positive h1 h2 h3 h4)
    (coveringPositive_cons (fun _ _ h1 h2 h3 h4 => Sendov911Box10_78.box_positive h1 h2 h3 h4)
    (coveringPositive_cons (fun _ _ h1 h2 h3 h4 => Sendov911Box10_79.box_positive h1 h2 h3 h4)
    (coveringPositive_nil))))))))))

end SendovN.Red10D

#print axioms SendovN.Red10D.eq_70
#print axioms SendovN.Red10D.red_70
#print axioms SendovN.Red10D.eq_71
#print axioms SendovN.Red10D.red_71
#print axioms SendovN.Red10D.eq_72
#print axioms SendovN.Red10D.red_72
#print axioms SendovN.Red10D.eq_73
#print axioms SendovN.Red10D.red_73
#print axioms SendovN.Red10D.eq_74
#print axioms SendovN.Red10D.red_74
#print axioms SendovN.Red10D.eq_75
#print axioms SendovN.Red10D.red_75
#print axioms SendovN.Red10D.eq_76
#print axioms SendovN.Red10D.red_76
#print axioms SendovN.Red10D.eq_77
#print axioms SendovN.Red10D.red_77
#print axioms SendovN.Red10D.eq_78
#print axioms SendovN.Red10D.red_78
#print axioms SendovN.Red10D.eq_79
#print axioms SendovN.Red10D.red_79
#print axioms SendovN.Red10D.chunkPos
